> [!info]- Tags
> #LangagesDeProgs #Ruby #ManipArr 

## each

Itère sur chaque élément du tableau.

```ruby
[1, 2, 3].each { |x| puts x }
```


## each_with_index

Itère sur chaque élément du tableau avec l'index.

```ruby
[a, b, c].each_with_index { |x, index| puts "#{index + 1} - #{x}" }
```
