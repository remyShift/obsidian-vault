> [!info]- Tags
> #LangagesDeProgs #Ruby #ManipArr 

## map

Applique un bloc de code à chaque élément et renvoie un nouveau tableau. --> **retourne un nouveau tableau**

```ruby
[1, 2, 3].map { |x| x * 2 } # => [2, 4, 6]
```
