---
tags: [LangagesDeProgs, JS, ManipObj]
---

## Object.create(prototype)

Crée un nouvel objet avec le prototype d'un objet existant.

```js
const proto = { a: 1 };
const obj = Object.create(proto);
console.log(obj.a); // 1
```
