> [!info]- Tags
> #LangagesDeProgs #JS #ManipStr

## split(separator, limit)

Divise une chaîne en un tableau de sous-chaînes en utilisant le séparateur spécifié.

```js
let str = "Hello World";
let array = str.split(" "); // ["Hello", "World"]
```
