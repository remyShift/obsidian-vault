---
tags: [LangagesDeProgs, JS, ManipArr]
---

## join()

Concatène tous les éléments d'un tableau en une chaîne de caractères, en utilisant un séparateur spécifié.

```js
let arr = [1, 2, 3];
let str = arr.join('-'); // "1-2-3"
```
