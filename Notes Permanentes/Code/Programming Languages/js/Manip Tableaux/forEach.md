> [!info]- Tags
> #LangagesDeProgs #JS #ManipArr 

## forEach(callback)

Exécute une fonction donnée pour chaque élément du tableau.

```js
let arr = [1, 2, 3];
arr.forEach(element => console.log(element)); // 1 2 3
```
