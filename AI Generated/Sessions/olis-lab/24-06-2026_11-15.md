---
date: 24-06-2026
heure: 11:15
project: olis-lab
source: ai
tags: [session, recap]
---

# Recap — olis-lab — 24/06/2026

## Avancées
- Cadrage complet de TASK-1115 (régénération du SKU) rédigé sous forme de brief de décision EN, suivant le template de ticket (Context / Acceptance Criteria / Scope / Implementation Notes / Dependencies) — fichier plan `hier-on-a-fait-glimmering-flamingo.md`, approuvé par Rémy.
- Exploration ciblée du code (4 sous-agents) : mécanique de génération du SKU, lock du champ, users/RBAC Payload, champ `adminLabel`, et flux SKU → BigBlue.
- Recommandation formulée et résumé EN prêt à coller pour Diego / le thread Slack.
- **Feature bulk-add "Add selected Products to an Edit"** (plan séparé `~/.claude/plans/en-suivant-le-template-rustling-toucan.md`) : plan EN au template avec comparatif Approche A (champ relation + bulk-edit natif) / B (composant + endpoint custom) / C (PATCH client) ; B recommandée.
- Vérifié à la source `@payloadcms/ui` (3.84.1) les APIs : `useSelection()`, slots list view, `useListDrawer`, sémantique du bulk-edit natif.
- Image annotée des 5 slots d'injection de la list view Products produite (`~/Desktop/products-slots-annotated.png`).
- Évalué la suggestion de Diego (lien doc Payload `relationship#bi-directional` → `join` field) : lecture doc + type `JoinField` installé ; rédigé un paragraphe EN "Companion" prêt à intégrer au plan + 2 micro-ajustements (Acceptance Criteria + Out of scope).

## Problèmes / Bugs
- **Duplicate casse le SKU** : `useAsTitle: 'sku'` → Payload append ` - Copy` sur le SKU du duplicate → SKU malformé, en collision `unique`, et non éditable (readOnly).
- **Aucune voie pour corriger un vrai mistake** : SKU gelé (readOnly + guard `if (data.sku || originalDoc?.sku) return data`), pas de chemin UI ; l'édition directe en DB ne crée pas de version Payload → ne persiste pas.
- **La PR auto-regen-on-brand-change (`f3ab29959`) revertée** (`46a14fbf`) : muter un SKU déjà engagé chez BigBlue = produit out-of-stock. Crainte de Michele confirmée dans le code.

## Découvertes
- **Le SKU est la clé de jointure avec BigBlue** : `apps/server` lit l'inventaire BigBlue et matche par SKU (`inventoryMap[sku]` dans `xmlController.js`, via `getAllInventoryFromBigblue()`). Aussi clé sur order items + bundles.
- **Le repo ne pousse JAMAIS de produits/SKU vers BigBlue** : intégration read-only (inventaire) + webhooks de statut de commande + push de bundles uniquement.
- **Aucun signal first-class "ce SKU est live sur BigBlue"** ; seuls proxys : `status` (`Live`/`Staged`/`Offline`) et `syncMetadata.lastSyncedAt`.
- `syncMetadata` = sync vers la **legacy MongoDB**, pas BigBlue (endpoint `/sync/product/:id`).
- **Pas de RBAC** : collection `Users` en auth nu (aucun champ custom), tous les users authentifiés = admin. Pattern `roles` documenté dans `apps/cms/AGENTS.md` mais non implémenté.
- Patterns réutilisables déjà présents : `syncButton` (`type: 'ui'` + composant `SyncProductButton`) et `CreateOnlyTextField` sur l'EAN → précédents directs pour une action manuelle et un champ conditionnel.
- La PR `feat/edits-product-picker-admin-label` déplace déjà `useAsTitle` de `sku` → `adminLabel` (champ caché, calculé) — utile pour le fix duplicate.
- Le vrai défaut de design : le gel du SKU se déclenche sur "une valeur existe" au lieu de "le produit est engagé downstream (BigBlue)".

## Décisions prises
- Périmètre de ce ticket = **cadrer le problème**, pas trancher la solution (décision en meeting).
- Reco : **découpler les deux besoins** — Option D (adminLabel + clear SKU au duplicate) pour le duplicate ; Option B (action manuelle, confirmée, downstream-aware, persistée via Payload) pour la correction ; Option C (RBAC / unlock SKU pour tech) **différée** comme chantier séparé.
- Contexte établi : **c'est l'équipe contenu / client qui édite les produits** → un bouton "régénérer" ouvert à tous est risqué, le gating par rôle a du sens à terme.
- Brief livré en anglais (thread Michele/Diego en EN).

## Prochaines étapes
- Apporter le résumé EN au thread / au meeting.
- Trancher avec Michele et Diego : Option A (auto en zone sûre) vs B (100% manuel) ; et si l'action manuelle doit être role-gated (→ RBAC maintenant ou plus tard).
- Décider : garder les proxys `status`/`syncMetadata` ou ajouter un vrai signal "live on BigBlue".
- Définir le comportement pour les **legacy SKUs manuellement overridés** si la dérivation est réactivée.
- Diego planifie l'implémentation selon la décision.
