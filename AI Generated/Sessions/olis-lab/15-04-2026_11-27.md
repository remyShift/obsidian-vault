---
date: 2026-04-15
heure: 11:27
project: olis-lab
source: ai
tags: [session]
---

# Recap — olis-lab — 15/04/2026

## Avancées
- Plan de tests unitaires rédigé pour le checkout guest (`apps/web/app/checkout-guest/`) : 4 modules couverts (`price-engine.ts`, `schemas.ts`, `useCheckoutSteps.ts`, `checkout.ts`), ~44 cas de tests définis
- Plan de tests E2E Playwright rédigé pour le même checkout : réduit à 1 seul test (happy path complet) suite au feedback de Kyle
- Exploration approfondie du codebase checkout-guest : pricing engine, schémas Zod, state machine du reducer, payload builder, intégration Stripe Elements
- MCP Obsidian configuré et fonctionnel via plugin MCP Tools (binaire stdio + API key Local REST API)
- MEMORY.md initialisé avec les standards Craft de Rémy et le setup MCP
- Setup MCP complet diagnostiqué : vrai fichier de config = `~/.claude.json`
- lean-ctx et symdex déplacés en User MCPs (globaux) dans `~/.claude.json` — disponibles dans tous les projets
- Bug d'encodage dans le skill `/recap` corrigé : utilise désormais `Write` filesystem direct au lieu de `mcp__obsidian__patch_vault_file`

## Problèmes / Bugs
- MCP Obsidian avec `@modelcontextprotocol/server-filesystem` impossible à faire pointer sur le vault : Claude Desktop override systématiquement le path avec le répertoire du projet courant quel que soit le fichier de config modifié (`claude_desktop_config.json`, `~/.claude/settings.json`, `~/.claude/mcp.json`)
- `mcp__obsidian__patch_vault_file` produit des doublons et des erreurs d'encodage sur les accents (ex: "Avancées" → "AvancÃ©es") — ne jamais utiliser pour écrire

## Découvertes
- Playwright est déjà installé dans `apps/cms/` — le config de `apps/web/` peut en être dérivé directement
- Le checkout tourne sur le port 3002 (`next dev -p 3002`)
- Les Stripe Elements sont dans des iframes — accès Playwright via `page.frameLocator('iframe[name^="__privateStripeFrame"]')`
- `createStepReducer` est une fonction pure testable sans React (si exportée) — isole la logique métier du hook RHF
- Le reducer utilise `ReadonlySet` pour `completedSteps` — important pour les assertions dans les tests
- Le vrai fichier de config MCP pour Claude Code est `~/.claude.json` (section `mcpServers`) — les autres fichiers sont soit pour Claude Desktop, soit ignorés
- Claude Desktop override le path du serveur filesystem avec le cwd du projet — sandboxing intentionnel, non contournable par config
- Le plugin MCP Tools d'Obsidian expose un serveur stdio natif (`/.obsidian/plugins/mcp-tools/bin/mcp-server`) avec des outils Obsidian natifs bien supérieurs au filesystem générique
- lean-ctx s'installe via `npm install -g lean-ctx-bin` (pas `lean-ctx`)
- lean-ctx et symdex configurés comme "Local MCPs" du projet `~` — pour les rendre globaux, les déplacer dans le `mcpServers` de niveau supérieur dans `~/.claude.json`
- claude-mem injecte ~800-1000 tokens à chaque démarrage de session — à évaluer vs. valeur réelle des observations capturées
- Argument défensif pour tester le reducer : depuis que le step gating a été retiré du scope E2E, c'est le seul endroit où `isAccessible` (invariant non-trivial) est couverte — sans ce test, un refactoring silencieux casse le gating sans qu'aucun test le détecte

## Décisions prises
- Tests unitaires : pas de nouveaux outils — Vitest suffit, tests colocalisés avec les sources
- Tests E2E : `@playwright/test` à ajouter en devDependency dans `apps/web/`, config copiée/adaptée depuis `apps/cms/`
- `buildExpressCheckoutPayload()` exclu des tests unitaires — trop couplé au runtime Stripe PaymentRequest
- Relay points exclus des E2E — dépendance Mapbox, différé
- `createStepReducer` doit être exporté pour être testable unitairement (actuellement interne)
- MCP Obsidian : utiliser le binaire MCP Tools plutôt que le serveur filesystem générique
- claude-mem : garder en observation 2-3 sessions avant de décider de le conserver ou non
- Stack mémoire retenu : Obsidian MCP (pull on-demand) + lean-ctx (réduction contexte) + claude-mem (observation) — pas redondant si chacun reste dans son rôle
- E2E réduit à 1 test (happy path) sur recommandation de Kyle : les cas d'erreur et le gating restent dans les tests unitaires

## Prochaines étapes
- Aligner avec Kyle sur le test du reducer (argument préparé : angle mort créé par la suppression du gating du scope E2E)
- Implémenter les tests unitaires (4 fichiers) selon le plan
- Implémenter les tests E2E Playwright (setup + 1 fichier spec)
- Exporter `createStepReducer` depuis `useCheckoutSteps.ts` si ce n'est pas déjà fait
- Ajouter `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` (`pk_test_...`) dans `apps/web/.env.local` pour les E2E
- Évaluer claude-mem après 2-3 sessions via http://localhost:37777
