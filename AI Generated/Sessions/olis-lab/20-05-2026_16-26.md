---
date: 20-05-2026
heure: 16:26
project: olis-lab
source: ai
tags: [session]
---

# Recap — olis-lab — 20/05/2026

## Avancees
- Diagnostic complet du bug PostHog : les events ne sont plus enregistres depuis ~1 mois
- Identification precise du commit responsable : `5db5dceee` (17 avril 2026) — bump posthog-js 1.335.2 -> 1.369.2
- Redaction d'un plan de diagnostic structure (Phase 1 : exploration codebase, Phase 2 : architecture du fix)
- Redaction d'un ticket complet en anglais avec contexte, acceptance criteria, scope et implementation notes
- Fourniture du lien direct vers le commit GitHub coupable
- Iteration du ticket suite au retour reviewer (Kyle) : clarification de la relation entre le version bump, `defaults`, et l'absence de re-application du consentement
- Redaction d'une reponse naturelle (non listee) au commentaire de code review, incluant l'explication de `defaults: '2025-05-24'` et un paragraphe final sur `cookieless_mode: 'on_reject'`
- Plan SearchController redige et approuve : endpoint unifie `/search/products-bundles/:query/:lang` avec entrelacement 1 bundle / 2 produits
- Backend : creation de `apps/server/controllers/searchController.js` (interleave + appels paralleles aux modeles existants)
- Backend : creation de `apps/server/routes/searchRouter.js` + enregistrement dans `server.js`
- Frontend : ajout des types `ProductSearchItem`, `BundleSearchItem`, `SearchResultItem` dans `pages/SearchPage/types.ts`
- Plan Payload blocks + page builder redige et approuve — couvre homepage et shop page via Globals CMS
- Tests CMS : choix final de strategie — `uniquePerCategory` testee en pur unitaire (pas d'integration Payload)
- Tests CMS : extraction de `checkUniquePerCategory` depuis `Subcategories.ts` — fonction pure avec `find` injecte en parametre explicite
- Tests CMS : 8 tests unitaires ecrits dans `tests/unit/uniquePerCategory.test.ts`, zero cast, zero mock de contexte Payload, 5ms
- Tests CMS : ajout de `test:unit` dans `package.json` avec config Vitest dediee `vitest.unit.config.mts`
- Tests CMS : redaction du message de PR et d'un commentaire explicatif sur les choix de types dans `Subcategories.ts`

## Problemes / Bugs
- **Bug principal** : `useCookieConsent.ts` (web_client) ne reapplique jamais le consentement au rechargement — `posthog.opt_in_capturing()` est uniquement appele lors d'une interaction avec la banniere, jamais au montage pour les utilisateurs ayant deja consenti
- **Bug aggravant** : posthog-js 1.369.2 enforce `defaults: '2025-05-24'` de facon stricte — chaque session demarre en etat "pending" et bloque les events jusqu'a ce que `opt_in_capturing()` soit appele dans la session courante
- **Bug aggravant** : `cookieless_mode: 'on_reject'` empeche PostHog de persister le flag opt-in en cookie/localStorage quand l'utilisateur est en etat "pending", ce qui brise la continuite entre sessions
- Meme bug present dans `useCookieConsent.tsx` (apps/web, Next.js) — fix different a cause du SSR (consent null au montage)
- **Bug search** : apres refactor frontend (queries unifiees), plus aucun resultat quelle que soit la query — root cause : probleme preexistant cote serveur (pas lie au code ecrit cette session)
- Frontend search refactor revert : `useSearchPageQuery.ts` et `SearchPage.tsx` reverts par le dev, les changements backend restent en place mais l'endpoint n'est pas encore utilise

## Decouvertes
- La configuration `defaults: '2025-05-24'` signifie "utiliser le modele de consentement explicite introduit par PostHog le 24 mai 2025" — en v1.335.2 c'etait lache, en v1.369.2 c'est strict
- `cookieless_mode: 'on_reject'` fait que PostHog n'ecrit rien en localStorage (y compris son propre flag opt-in) tant que l'utilisateur est en etat "pending" — rend le re-apply au montage indispensable, pas optionnel
- L'AnalyticsService (packages/shared) est un singleton statique qui wrappe PostHog + GA4 + Meta Pixel — le fix sur `updateConsent()` se propage automatiquement aux 3 outils
- Le hook `web_client/useCookieConsent.ts` importe un fichier local (`service/analytics/AnalyticsService.ts`) qui re-exporte simplement depuis `@olis-lab/shared/analytics` — ce n'est pas un shadowing problematique
- La logique de persistance PostHog est entierement a la charge de l'app (PostHog ne lit plus son propre cookie d'opt-in dans cette configuration)
- Le `posthog.init()` dans `index.tsx` est conditionne par `Object.keys(envOverrides).length > 0` — fragile mais actuellement safe pour tous les envs
- `shopProductSchema.getSearchProducts` et `Bundles.getSearchBundles` ajoutent deja `type: 'product'` / `type: 'bundle'` sur chaque item retourne — pas besoin de l'ajouter dans le controller
- `isBundle` n'est pas dans le `select` MongoDB de `getSearchBundles` → il faut le deriver de `type` cote frontend
- Pas d'ErrorBoundary sur SearchPage : un crash JS montre une page blanche, pas un etat "no results"
- Payload blocks : champ de type `blocks` = union typee stockee en tableau JSON, reorderable drag-and-drop dans l'admin, parfait pour page builder
- Payload Globals : singleton (une seule entree, pas une collection) — adapte pour homepage, shop page, footer. Actuellement `globals: []` dans `payload.config.ts`
- Tests : `as unknown as T` est inevitable pour les mocks partiels de types complexes — sauf si on extrait la logique en fonction pure avec injection de dependance explicite

## Decisions prises
- Ne pas modifier `defaults: '2025-05-24'` ni `cookieless_mode: 'on_reject'` : les deux sont RGPD-corrects, le bug est ailleurs
- Fix minimaliste : ajouter un `useEffect` de montage dans chaque hook pour reappliquer le consentement stocke
- Pour `web_client` : `useEffect(() => {...}, [])` (consent synchrone)
- Pour `apps/web` : `useEffect` avec `useRef` pour detecter le premier chargement du consent (asynchrone a cause du SSR)
- Dans le ticket et la reponse de review : ne pas lister les 3 bugs separement — expliquer la relation causale de facon narrative (version bump = trigger, bug latent = cause, cookieless_mode = aggravant explique en dernier paragraphe)
- SearchController : pas de fichier `SearchApi.ts` dedie — l'appel axios se fait directement dans le hook
- Algorithme d'entrelacement : 1 bundle toutes les 2 produits ; bundles restants en fin de liste
- Blocks : cibler `apps/web` (Next.js) uniquement ; shop page avant homepage (scope plus petit)
- Tests : extraction de la logique dans `checkUniquePerCategory` avec `find` en parametre — injection de dependance explicite plutot que mock de contexte

## Prochaines etapes
- Resoudre le probleme serveur preexistant avant de retester l'endpoint search unifie
- Implementer le fix PostHog dans `apps/web_client/src/components/CookieBanner/useCookieConsent.ts`
- Implementer le fix PostHog dans `apps/web/components/layout/CookieBanner/hooks/useCookieConsent.tsx`
- Verifier sur staging : `posthog.has_opted_in_capturing()` doit retourner `true` au chargement
- Ouvrir PR PostHog + ticket Linear
- Merger `chore/cms-int-tests-ci` : PR description redigee, attente retour Diego
- Blocks (quand le moment vient) : suivre les 8 etapes du plan — commencer par definir les block types dans `apps/cms/src/blocks/`
- Resoudre bug S3 double declaration XML (bloquant pour automatisation feeds)
- Implementer CRON + feed Meta
