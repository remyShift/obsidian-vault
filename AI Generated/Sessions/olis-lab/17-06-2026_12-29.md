---
date: 17-06-2026
heure: 12:29
project: olis-lab
source: ai
tags: [session]
---

# Recap — olis-lab — 17/06/2026

## Avancées
- Fix du slugify Payload (bug accents perdus + tirets multiples + tirets en bord). Branche `fix/payload-slugs-generation`, PR ouverte.
- Nouvel utilitaire partagé `apps/cms/src/lib/slugify.ts` (normalize NFD + strip diacritiques + collapse tirets + trim bords). Conserve le strip de `/` et `.` (comportement existant préservé).
- Brands : `slugField({ fieldToUse: 'name', slugify: ... })` → corrige via l'option native, sans toucher node_modules.
- Products : suppression du slugify local dupliqué, import du partagé (le hook `generateBrandTitleSlug` inchangé).
- Migration `20260617_100000_fix_slugs.ts` (enregistrée dans `migrations/index.ts`) : recompute à changement minimal (détecte la locale source qui reproduit le slug stocké, fallback sur fix du préfixe brand + dashFix). Logger enrichi : `sku`, `id`, source (`en`/`fr`/`fallback`), `"old" -> "new"`.
- Ajout d'un WARN dans la migration : flague tout slug non reproductible depuis le titre actuel (anti-trou silencieux).
- Inventaire complet produit (233 produits scannés) : 206 conformes, 26 produits + 1 brand à changer = 27 redirections.
- Message de PR rédigé (template Notion) + comparaison inventaire vs migration réelle prod.
- (Footer global) Plan complet rédigé et validé pour une nouvelle global Payload `footer` qui centralise le contenu éditorial du footer : image de fond, textes newsletter, colonnes de liens, liens légaux, réseaux sociaux, tagline, copyright.
- (Footer global) Deux livrables produits : le schéma complet (code `Footer.ts` prêt à coller, avec sous-champs de lien réutilisables) + un schéma "light" en tableau, en anglais, pour le ticket Notion.
- (Footer global) Exploration des conventions existantes pour calquer le pattern : globals `AnnouncementBar`/`TradingPlan`, `isValidLink`, type guards (`createAsserter`/`assert.media`), fetch `cmsClient.findGlobal`.

## Problèmes / Bugs
- **Misaj** (`misaj-glow-protect-rpulsif-100-naturel`) : slug cassé NON corrigé par la migration sur prod. Le titre a été entièrement réécrit (`Glow Protect Répulsif 100%` → `Body Moisturiser & Natural Repellent`), donc la source de l'accent a disparu → impossible à récupérer auto. Flaggé pour re-save manuel.
- `dist` de `packages/shared` périmé (`assertMedia` absent) → erreur ESM au `payload migrate`. Résolu par `pnpm --filter @olis-lab/shared build`.
- Migration uniquement exécutable en **Node 20** (nvm), pas Node 25 (système).
- `nvm use` dans un pipe = subshell → PATH non propagé (piège shell).
- `migrate:down` a temporairement rollback la migration backfill (adminLabel) ; ré-appliquée par le `up` suivant, aucun dégât (19 subcategories OK).

## Découvertes
- En JS `\w` = `[A-Za-z0-9_]` → inclut les chiffres. Le slugify ne supprime PAS les nombres (clarification demandée par Rémy).
- Tous les slugs produits ne dérivent pas de la même locale : 1 seul vient du FR (Misaj), les 23 autres de l'EN. D'où la stratégie locale-source.
- Sync Payload → legacy DB (`shop_products`) est **manuelle** (bouton admin → `POST /api/sync/product/:id`), pas d'`afterChange`. Un `payload.update` ne propage pas tout seul. Brands : pas de slug dans le shape legacy (`brand: brand.en.name`), rien à propager.
- `apps/server/utils/generateSlug.js` (Express legacy) a le même bug mais n'est pas le chemin qui fait foi → hors scope.
- (Footer) La collection Users du CMS est nue (`auth: true` + email seul), **aucun rôle / RBAC** → une permission par rôle (ex. verrouiller le champ `target` à un dev) nécessiterait d'abord d'ajouter le RBAC, ce n'est pas un petit ajout.
- (Footer) Le footer Next porte déjà un TODO explicite "Pass image from CMS" (background image = URL S3 hardcodée).
- (Footer) Liens footer aujourd'hui = label en i18n + destination (`path` + `app: next/legacy/external`) dans `routes.ts`. Trust bar et icônes de paiement sont liés à des composants (Lucide / SVG), pas du contenu éditorial.
- (Footer) `apps/web/lib/api/cms.ts` n'a pas de helper `findGlobal` (seules les collections sont gérées) → à ajouter.

## Décisions prises
- Scope slugify = **cms uniquement** (pas le serveur Express legacy).
- Slugify : conserver le comportement sur `/` et `.` (strip, pas remplacement par tiret) → ne change pas les brands non visées (ex. `Mid/night 00.00`).
- Migration = changement minimal préservant la locale source (pas de recompute en bloc qui casserait les URLs figées et nos décisions).
- Misaj et cas similaires (titre disparu) : **warn + re-save manuel admin**, pas de recompute auto en bloc.
- `grown-alchemist-skin-renewal-toner` (slug propre, titre dérivé +Mist) : laissé tel quel, pas un bug.
- `face-body-serum` : fix minimal (pas d'ajout du préfixe brand `irritated-`).
- Pas de tests automatisés pour ce fix (demande de Rémy).
- Propagation legacy = étape séparée via sync existant (pas de nouveau code legacy).
- (Footer) Liens de navigation **INCLUS en CMS** (choix de Rémy, malgré ma réserve sur le couplage au routing de migration).
- (Footer) Icônes (trust bar, paiement) et logo SVG : **restent en code**.
- (Footer) Section "More" auth-dépendante : modélisée en CMS via un flag `visibility` (`always`/`loggedOut`/`loggedIn`), le FE filtre selon l'état d'auth (seule la logique de sélection reste en code).
- (Footer) Seed initial : **pas bloquant**, le footer CMS passera derrière un feature flag → seed à la main avant de flipper le flag.
- (Footer) Points laissés **ouverts sur le ticket** (non tranchés) : (1) `target` éditable par l'éditrice vs permission par rôle vs dérivation par path ; (2) année copyright figée vs préfixée en code.

## Prochaines étapes
- Avant deploy prod : **redirections CloudFront live AVANT la migration** (Rémy + Michele/Diego). 27 redirects, map = inventaire Notion.
- Misaj : re-save en admin après migration prod → vérifier `misaj-body-moisturiser-natural-repellent` et caler la redirect dessus.
- Séquence prod : redirects en place → migration Payload prod → propagation legacy via bouton/endpoint sync pour les 26 produits.
- Suivre la review de la PR `fix/payload-slugs-generation`.
- (Footer) Attendre les décisions de review sur les 2 questions ouvertes du ticket avant d'implémenter les parties concernées.
- (Footer) Quand validé, implémenter : `Footer.ts` + RowLabels + registre `payload.config.ts` + type guard `guardFooter` dans `packages/shared` + helper `getGlobal` côté `apps/web` + hook `useFooterQuery` côté `apps/web_client`.
