---
date: 01-05-2026
heure: 11:16
project: olis-lab
source: ai
tags: [session, recap]
---

# Recap — olis-lab — 01/05/2026

## Avancées
- Migration du plugin SEO : remplacement de `@payloadcms/plugin-seo` par `@payload-enchants/seo` dans `apps/cms`
- Installation de `@payload-enchants/seo` et `openai` dans `apps/cms` via pnpm
- Mise a jour de `payload.config.ts` : import renomme (`seoPlugin` → `seo`), ajout de `openaiApiKey`, ajout de `generateTitleAi` et `generateDescriptionAi` avec prompts cosmetiques adaptes EN/FR
- Zero erreur TypeScript apres corrections (types inferences via `args`, suppression de l'import `dist/types` non accessible)
- Redaction d'un message equipe en anglais expliquant le probleme de compatibilite et la solution via `pnpm patch`

## Problemes / Bugs
- `@payload-enchants/seo@1.2.2` compile avec Payload `3.0.0-beta.61` → incompatible avec Payload `3.79.1` stable
- `useFieldProps` supprime de `@payloadcms/ui` en stable, remplace par `useFieldPath` (retourne une `string` directe, plus un objet `{ path }`)
- `Cannot find module '@payload-enchants/seo/dist/types'` : chemin non expose dans l'`exports` map du package, inaccessible avec `moduleResolution: bundler`
- `fields` dans le nouveau plugin est un tableau statique (AJOUTE des champs) et non une fonction de filtrage comme dans l'ancien plugin → impossible de masquer le champ `preview` de la meme facon
- `seoPlugin` n'est plus le nom d'export : le package exporte `seo` uniquement

## Decouvertes
- `@payload-enchants/seo@1.2.2` a ete publie avec des peer deps pointant vers `3.0.0-beta.61`, ce qui le rend incompatible avec toute version stable de Payload 3.x
- `useFieldPath` (nouveau) retourne directement une `string`, contrairement a `useFieldProps` (ancien) qui retournait `{ path: string }`
- L'option `fields` du nouveau plugin fait un spread (`...pluginConfig?.fields || []`) : elle ajoute des champs supplementaires, pas de remplacement/filtrage des champs par defaut
- `pnpm patch` est le mecanisme standard pour patcher un package tiers sans le forker : cree un fichier `.patch` versionne dans le repo, applique automatiquement a chaque `pnpm install`
- Message envoye au mainteneur du plugin pour signaler l'incompatibilite Payload stable

## Decisions prises
- Utiliser `async (args) => { ... args.locale ... args.collectionSlug ... }` pour les callbacks AI afin d'eviter les `any` TypeScript sans import de types externe
- Supprimer l'option `fields` (filtrage du champ `preview`) car l'API du nouveau plugin ne le permet pas nativement
- Ne pas appliquer le patch `pnpm patch` avant la reponse du mainteneur — attendre un fix officiel ou une confirmation qu'un patch est necessaire
- Prompts AI contextuels : langue basee sur `locale` (fr/en), mention explicite d'Oli's Lab, regles de longueur (50-60 chars titre, 150-160 chars description)

## Prochaines etapes
- Attendre la reponse du mainteneur de `@payload-enchants/seo` sur le bug de compatibilite Payload stable
- Si pas de fix rapide : appliquer le `pnpm patch` (remplacer `useFieldProps` par `useFieldPath` dans `MetaTitle.js`, `MetaDescription.js`, `MetaImage.js` + corriger l'import)
- Tester la generation IA dans le CMS admin une fois le patch applique (bouton "Generate with AI" sur title et description)
- Verifier que l'onglet SEO apparait correctement sur les 4 collections (categories, subcategories, brands, products)
- Regenerer l'`importMap` apres correction du plugin (`pnpm generate:importmap`)
