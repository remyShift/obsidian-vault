---
date: 22-06-2026
heure: 23:38
project: seed4t-perso
source: ai
tags: [session]
---

# Recap — seed4t-perso — 22/06/2026

## Avancées
- Dupliqué le code domaine de seed4t (ex ts-seed) dans un repo perso isolé du repo mob : `Brick.ts`, tests, `index.ts`, `SPECS.md` copiés à l'identique.
- Repo local `seed4t-perso` (`~/Desktop/everything/Code/seed4t-perso`) + repo GitHub privé `remyShift/seed4t`, branche `main`, commit initial propre (pas l'historique mob).
- Migration Jest → Vitest 4 (`globals: true`, `@vitest/coverage-v8`), suppression de jest/ts-jest/@types/jest.
- Ajout ESLint 10 (flat config `eslint.config.mjs`) + typescript-eslint 8.62 + Prettier séparé avec `eslint-config-prettier`. `.prettierrc` calé sur le style existant (double quotes, 2 espaces, semis) → zéro churn sur le domaine.
- CI GitHub Actions `pipeline.yml` : lint → format:check → build → test:run. Build via `tsconfig.build.json` (exclut les tests).
- Vérifs de bout en bout vertes : 4 tests OK, build OK, lint clean, format clean (seul `renovate.json` reformaté).
- README réécrit deux fois : d'abord traduit en anglais, puis recentré sur le but/principe (composer un projet à partir de bricks plutôt que tailler dans un boilerplate) au lieu de décrire la stack.
- `SPECS.md` transformé en liste de tests vivante (style Beck) en anglais : vision + phases 1→4, chaque phase explique ce qu'elle fait émerger comme concept/archi, specs en checkboxes T1→T11 + décisions ouvertes.

## Problèmes / Bugs
- Homebrew `node@24` cassé (libllhttp manquante) → fallback sur le node de mise.
- Node local 21.5.0 incompatible avec Vitest récent (veut 18/20/22+) → épinglé Node 20.19.5 (`.nvmrc` + `.tool-versions`), aligné en CI.
- macOS case-insensitive : dossier `seed4t` entrait en collision avec `seed4T` existant → dossier local nommé `seed4t-perso` (repo GitHub reste `seed4t`, case-sensitive côté remote).
- Hot cache global périmé : annonçait "vitest/ts-seed", or l'original était TS6 strict + Jest 30 + ts-jest.

## Découvertes
- Stack réelle de l'original : TypeScript 6 strict + Jest 30 + ts-jest, zéro dépendance runtime, pas de linter (≠ ce que disait le cache).
- Limites du domaine actuel à corriger en TDD : `CatalogBrick { dependant: Brick | null }` ne gère qu'une dépendance sur un seul niveau (pas de transitivité ni deps multiples), pas de dédup au `Cart.add`, `Cart.remove` ignore les dépendances, aucune sortie réelle.
- Principe d'émergence : le domaine est pur aujourd'hui (rien sur les bords), donc pas encore d'hexagone — sain. Le 1er port émerge à la résolution de version (T8, `VersionResolver`), le 2e à la sortie de recette (T11, `RecipeWriter`). La structure de dossiers hexagonale vient en dernier (Phase 4), pas avant 2 ports concrets.

## Décisions prises
- Posture mentor stricte : Claude ne code jamais sur seed4t, il guide et revue ; Rémy écrit chaque test (red → green → refactor).
- Repo perso = copie isolée, le repo mob reste intact. GitHub privé.
- Migration vers Vitest sur le repo perso (le repo mob garde Jest).
- Stockage des deps prévu par nom (`string[]`) plutôt que par référence `Brick` (catalogue = source de vérité).
- Docs du repo en anglais (README + SPECS) pour cohérence/portfolio.
- Rémy a retiré la Phase 0 (hygiène) de `SPECS.md` pour garder une test-list strictement comportementale (Phases 1→4 conservées).

## Prochaines étapes
- Hygiène avant de coder : renommer `src/calc.test.ts` → `src/Brick.test.ts`, confirmer/fixer le nom dans `package.json` (résidu `ts-template-with-jest-tests`).
- Commit + push des changements docs (README + SPECS) et vérifier que le run CI passe au vert sur GitHub.
- Démarrer le TDD sur T1 (dédup catalogue) : un rouge → vert → refactor, puis montrer le cycle à Claude pour revue.
- Trancher au fil des tests : règle de `remove` (T7, GC orphelins vs interdiction), comportement sur cycle (T6, erreur vs stop), source de "latest" (registry npm vs table figée).
