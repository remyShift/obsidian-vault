---
date: 20-06-2026
heure: 10:58
project: obsidian-vault
source: ai
tags: [session, recap, shell, setup, evolve]
---

# Recap — obsidian-vault — 20/06/2026

> Deux temps dans la journée. **Matin (10:58)** : couche d'outils post-setup shell + bascule des helpers git de gum vers fzf. **Midi (11:45)** : passe `/evolve` complète + application, durcissement du skill evolve, et création/enrichissement de la note permanente **Setup Shell** (la matière du matin a bien été versée dans la note). Voir Prochaines étapes.

## Avancées

### Nouveaux outils installés (rôle de chacun) — matin
- **ripgrep (`rg`)** v15.1 : remplace/complète `grep -r`, cherche dans le **contenu** des fichiers, respecte `.gitignore`, saute `node_modules`/`dist` automatiquement. C'est le binôme de `fd` (fd = fichiers, rg = contenu). Exemples : `rg "adminLabel"`, `rg useCart -t js -l`.
- **fzf** v0.73 : fuzzy finder. Câblé dans fish (voir Fichiers). Keybinds : `Ctrl+R` (historique cherchable en fuzzy), `Ctrl+T` (insère un chemin de fichier avec preview bat), `Alt+C` (saut de dossier, **dormant sur Mac**). Source = `fd`, previews via `bat`/`lsd`.
- **xh** v0.26 : requêtes HTTP lisibles, alternative moderne à `curl` (réponse colorée/formatée auto). Pour tester l'API v2 Oli's Lab. Ex : `xh localhost:3000/api/products`, `xh POST localhost:3000/api/login email=x@x.com password=secret`.
- **tealdeer (`tldr`)** v1.8 : exemples concrets par commande au lieu du `man`. Cache local à peupler via `tldr --update`. Ex : `tldr git rebase`.
- **btop** v1.4.7 : moniteur système temps réel (CPU par cœur, RAM, réseau, disque, process triables/filtrables). Comble le trou "moniteur live" (ncdu ne fait que le disque statique). Thème **Dracula** (bundlé), fond transparent pour hériter de la translucidité Ghostty. Touches : `f` filtre, `k` tue, souris OK, `q` quitte.

### Helpers git fish : gum → fzf — matin
- **`gco`** (switch de branche) et **`gbd`** (suppression de branche) réécrits en **fzf avec preview du log** (`git log --oneline --color=always -20 {}`, preview-window right 55%). Avantage tueur sur `gum filter` : voir les derniers commits d'une branche en la survolant. Pour `gbd`, on voit ce qu'on jette avant de confirmer.
- `git checkout` → `git switch` (verbe moderne dédié).
- **`gadd`** (multi-sélection de fichiers) et **`gcim`** (saisie du message) **gardés en gum** : c'est leur terrain (multi-select + input texte), fzf n'apporte rien.

### Versionnement (chezmoi → remyShift/dotfiles) — matin
- 4 commits propres, **aucun avec mention IA** (conforme mémoire) : add 4 outils + wire fzf ; switch gco fzf ; switch gbd fzf ; add btop Dracula.
- Tout dans le Brewfile → machine coréenne récupère tout d'un `brew bundle`.

### Passe /evolve complète + application — midi
- **/evolve** : analyse cross-projet déléguée (3 sous-agents `Explore` : olis-lab, obsidian-vault, conversations). 15 sources nouvelles depuis le rapport du 13-06. Rapport `AI Generated/Evolve Reports/20-06-2026_evolve.md` (2 memory updates, 1 note, audit). Sommaire Evolve mis à jour.
- **/evolve --apply** (M1, M2, V1, A6 acceptés) :
  - **M1** `project_olis_lab.md` : bloc « Chantiers » rafraîchi (12-06 → 20-06). Clos : SKU Design A, announce bar, adminLabel picker, tests cart, fix CRA→PDP. Nouveaux : navbar global (en review), footer global (doc-first), génération/migration des slugs. + 2 conventions durables (relation Payload orpheline → string même à `depth>=1` ; `legacyId` = ObjectId de la sync).
  - **M2** `reference_olis_lab_build_traps.md` : ligne `importMap.js` précisée (régénère sur import nouveau, `git reset --hard` avant) + nouveau piège Tailwind v4 `@source` (composant rendu mais non stylé).
  - **V1** : création de la note permanente Setup Shell.
  - **A6** : backlink `[[Setup Shell]]` + checklist cochée dans la note veille `Pimp my shell (Julien Wittouck)`.
- **A5 appliqué** : nettoyage de `Skills/wrap/SKILL.md` (cadratins `—` → `-` dans les templates de nom de fichier + titre H1, règle ligne 114 reformulée pour ne plus se contredire).

### Durcissement du skill /evolve — midi
- 4 patches à `~/.claude/commands/evolve.md` pour que les faux positifs d'audit cessent de revenir : Phase 1a lit les items clos du dernier rapport (par **sujet stable**, pas par ID) ; Axe C **croise les memory files + la liste d'exclusion** avant de flagger « travail perdu » ; template du rapport gagne une section cumulative `Audit clos (ne plus re-signaler)` ; `--apply` écrit `resolved_ids`.
- Registre `Audit clos` amorcé dans le rapport 20-06 (3 sujets : recap 22-05 déplacé, docs ts-seed supprimés volontairement, notes ingredient-manager rangées dans Rating Tool).

### Note "Setup Shell" enrichie — midi
- Note créée, puis **renommée et déplacée par Rémy** : `Notes Permanentes/Code/Setup Shell.md` (ex « Setup Shell Mac » dans Vie Perso).
- Table sèche → **sections détaillées par outil** (description + commandes clés + 1 exemple), groupées par usage (terminal/shell, naviguer/lister, lire/chercher, système/réseau, env-runtimes-dotfiles, scripts/démarrage). 16 outils. `splashboard` documenté depuis son `--help` réel.
- Section **« Alias et fonctions maison »** ajoutée depuis les vrais `conf.d/aliases.fish` + `conf.d/gum.fish` : table d'alias (`cat`→bat, `ls/l/la/lla/lt`→lsd, `find`→fd, `code`→zed, `cd`→zoxide, abbr `gcd`) + détail des 4 fonctions git (gco/gbd fzf, gadd/gcim gum).

## Problèmes / Bugs
- **Faux positifs d'audit /evolve** : la passe a re-flaggé A1 (docs ts-seed) et A4 (notes ingredient-manager) comme « travail perdu » alors qu'ils étaient déjà réglés **et documentés dans les memory** (`project_ts_seed.md` = « supprimés volontairement », `project_ingredient_manager.md` = chemin réel `Projets/Oli's Lab/Scientific/Rating Tool/`). Cause : l'audit ne croisait ni les memory ni les rapports précédents. Corrigé (voir durcissement evolve).
- **Incohérence note vs config réelle** : le recap du matin affirmait `rg`/`xh`/`fd` non aliasés ; en réalité `find`→`fd` ET `cat`→`bat` SONT aliasés dans `aliases.fish`. La config étant la source de vérité, la note a été corrigée (`rg` et `xh` restent non aliasés, eux).
- (Matin) Aucun bug technique : installs, câblage fish et commits OK du premier coup.

## Découvertes
- **fzf 0.73** s'installe dans fish via `fzf --fish | source` (méthode moderne, même logique que `mise activate` / `starship init` / `zoxide init`).
- **Alt+C dormant sur Mac/Ghostty** : Option réservé aux accents (`macos-option-as-alt = false`), le binding fzf de saut de dossier ne se déclenche pas. `Ctrl+R`/`Ctrl+T` marchent. Pas grave : zoxide (`cd`) couvre. Noté en commentaire dans la config.
- **btop bundle ~45 thèmes** dans `/opt/homebrew/share/btop/themes/` (dont `dracula.theme`) → pas de téléchargement (respecte « rien en curl »).
- **tealdeer** affiche « page not found » tant que `tldr --update` n'a pas peuplé le cache.
- **Les rapports /evolve sont déjà le registre d'état de l'audit** : il suffit qu'evolve les relise. Pas besoin d'un memory dédié pour « se souvenir » qu'un faux positif est réglé.
- **splashboard CLI** : `init` (snippet shell à sourcer), `install` (onboarding interactif), `watch` (plein écran live), `catalog`, `cache`, `trust` (consentement widgets réseau).
- `alias find fd` existe mais **fd n'a pas la syntaxe de find** (seul le nom est repris) — piège noté dans la config.

## Décisions prises
- **fzf recouvre gum pour la sélection fuzzy de branche** → `gco`/`gbd` basculés. gum conservé là où il est meilleur (multi-select `gadd`, input `gcim`).
- **`rg` et `xh` NON aliasés** sur `grep`/`curl` : commandes en plus, pas remplacements → scripts existants inchangés. (Contraste avec `cat`→bat, `find`→fd qui le sont.)
- **Brewfile édité à la main** (pas `brew bundle dump --force`) pour préserver structure thématique + commentaires.
- **btop en Dracula + fond transparent** (`theme_background = False`) pour cohérence visuelle avec Ghostty/starship/bat.
- **Outils écartés à l'audit (avec raison)** : `direnv` (mise gère `[env]` par dossier), `eza` (lsd déjà là), `dust`/`duf` (ncdu), `television` (= fzf), `fastfetch`/`onefetch` (splashboard), `delta`/`lazygit`/`glow`/`yq` (non retenus), `atuin` (recoupe Ctrl+R fzf).
- **Candidats gardés sur la table** : `yazi` (file manager TUI), pimper `starship.toml` (icônes Dracula).
- **Ne pas tout mettre en memory file** (feedback explicite de Rémy : « tu te reposes un peu trop là-dessus »). Un faux positif d'audit se reclasse **dans le rapport /evolve** (section Audit clos), pas dans un memory. Les memory ne sont pas un registre d'état mouvant.
- **A1 / A4 = faux positifs définitivement clos** (actés dans les memory existants + registre Audit clos du rapport 20-06).
- **Niveau de détail de la note Setup Shell** : description + commandes principales + 1 exemple par outil, + section alias/fonctions maison.

## Prochaines étapes
- **Vérifier au prochain `/evolve` que A1/A4 ne remontent plus** (test réel du durcissement de l'audit).
- Reliquat du 19/06 (côté Rémy, toujours pas fait) : `sudo rm -rf` python.org, rotation du mot de passe MongoDB Atlas (fuité en clair), suppression du backup `~/shell-migration-backup-*`, tester `/coach` en réel.
- Optionnel note Setup Shell : ajouter le bloc **PATH/env** de `config.fish` (brew shellenv, `~/.local/bin`, `~/.cargo/bin`, `ANDROID_HOME` + platform-tools) pour compléter le volet reproductibilité.
- Reliquat audit /evolve **manuel** (non auto-fix) : A3 (wikilinks `Notes de Lecture/Code/` à vérifier/promouvoir).
- Shell, si envie : installer `yazi` (+ câblage zoxide/fzf), ou pimper `starship.toml` Dracula avec icônes par langage.

## Fichiers de config touchés (référence pour la note)
- `~/.config/fish/config.fish` : section **fzf** ajoutée (`fzf --fish | source` + 6 vars `FZF_*`, source = fd, previews bat/lsd).
- `~/.config/fish/conf.d/aliases.fish` : alias binaires boostés (`cat`→bat, `ls`/`l`/`la`/`lla`/`lt`→lsd, `find`→fd, `code`→zed) + abbr `gcd`.
- `~/.config/fish/conf.d/gum.fish` : `gco` + `gbd` en fzf (preview log) ; `gadd`/`gcim` en gum.
- `~/Brewfile` : +5 brews commentés (ripgrep/fzf/xh/tealdeer + btop).
- `~/.config/btop/btop.conf` : `color_theme = "dracula"`, `theme_background = False`, `truecolor = True`.
- **Hors machine (vault/skill)** : `~/.claude/commands/evolve.md` (4 patches audit), `Skills/wrap/SKILL.md` (cadratins nettoyés), memory `project_olis_lab.md` + `reference_olis_lab_build_traps.md`, note `Code/Setup Shell.md`.
