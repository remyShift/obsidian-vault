---
date: 20-06-2026
heure: 10:58
project: obsidian-vault
source: ai
tags: [session, recap, shell, setup, evolve, harnais, splashboard]
---

# Recap — obsidian-vault — 20/06/2026

> Plusieurs temps dans la journée. **Matin (10:58)** : couche d'outils post-setup shell + bascule des helpers git de gum vers fzf. **Midi (11:45)** : passe `/evolve` complète + application, durcissement du skill evolve, et création/enrichissement de la note permanente **Setup Shell**. **Suite (12:11)** : migration complète zsh → fish (fish promu login shell, pnpm câblé, `.zshrc`/oh-my-zsh supprimés, ménage des reliquats zsh). **Suite (12:28) - session `/improve`** : traitement du backlog harnais (hook git anti co-author, `rm -i`, node 20 olis-lab), push des dotfiles, et déplacement du backlog vers `AI Generated/`. **Après-midi (14:16)** : deux dashboards **splashboard** entièrement custom (home + project), long debug du token GitHub résolu via `secrets.toml`, note Setup Shell mise à jour, dotfiles poussés. Voir Prochaines étapes.

## Avancées

### Nouveaux outils installés (rôle de chacun) — matin
- **ripgrep (`rg`)** v15.1 : remplace/complète `grep -r`, cherche dans le **contenu** des fichiers, respecte `.gitignore`, saute `node_modules`/`dist` automatiquement. C'est le binôme de `fd` (fd = fichiers, rg = contenu). Exemples : `rg "adminLabel"`, `rg useCart -t js -l`.
- **fzf** v0.73 : fuzzy finder. Câblé dans fish (voir Fichiers). Keybinds : `Ctrl+R` (historique cherchable en fuzzy), `Ctrl+T` (insère un chemin de fichier avec preview bat), `Alt+C` (saut de dossier, **dormant sur Mac**). Source = `fd`, previews via `bat`/`lsd`.
- **xh** v0.26 : requêtes HTTP lisibles, alternative moderne à `curl` (réponse colorée/formatée auto). Pour tester l'API v2 Oli's Lab. Ex : `xh localhost:3000/api/products`, `xh POST localhost:3000/api/login email=x@x.com password=secret`.
- **tealdeer (`tldr`)** v1.8 : exemples concrets par commande au lieu du `man`. Cache local à peupler via `tldr --update`. Ex : `tldr git rebase`.
- **btop** v1.4.7 : moniteur système temps réel (CPU par cœur, RAM, réseau, disque, process triables/filtrables). Comble le trou "moniteur live" (ncdu ne fait que le disque statique). Thème **Dracula** (bundlé), fond transparent pour hériter de la translucidité Ghostty. Touches : `f` filtre, `k` tue, souris OK, `q` quitte.

### Helpers git fish : gum → fzf — matin
- **`gco`** (switch de branche) et **`gbd`** (suppression de branche) réécrits en **fzf avec preview du log** (`git log --oneline --color=always -20 {}`, preview-window right 55%). Avantage tueur sur `gum filter` : voir les derniers commits d'une branche en la survolant. Pour `gbd`, on voit ce qu'on jette avant de confirmer.
- `git checkout` → `git switch` (verbe moderne dédié).
- **`gadd`** (multi-sélection de fichiers) et **`gcim`** (saisie du message) **gardés en gum** : c'est leur terrain (multi-select + input texte), fzf n'apporte rien.

### Versionnement (chezmoi → remyShift/dotfiles) — matin
- 4 commits propres, **aucun avec mention IA** (conforme mémoire) : add 4 outils + wire fzf ; switch gco fzf ; switch gbd fzf ; add btop Dracula.
- Tout dans le Brewfile → machine coréenne récupère tout d'un `brew bundle`.

### Passe /evolve complète + application — midi
- **/evolve** : analyse cross-projet déléguée (3 sous-agents `Explore` : olis-lab, obsidian-vault, conversations). 15 sources nouvelles depuis le rapport du 13-06. Rapport `AI Generated/Evolve Reports/20-06-2026_evolve.md` (2 memory updates, 1 note, audit). Sommaire Evolve mis à jour.
- **/evolve --apply** (M1, M2, V1, A6 acceptés) :
  - **M1** `project_olis_lab.md` : bloc « Chantiers » rafraîchi (12-06 → 20-06). Clos : SKU Design A, announce bar, adminLabel picker, tests cart, fix CRA→PDP. Nouveaux : navbar global (en review), footer global (doc-first), génération/migration des slugs. + 2 conventions durables (relation Payload orpheline → string même à `depth>=1` ; `legacyId` = ObjectId de la sync).
  - **M2** `reference_olis_lab_build_traps.md` : ligne `importMap.js` précisée (régénère sur import nouveau, `git reset --hard` avant) + nouveau piège Tailwind v4 `@source` (composant rendu mais non stylé).
  - **V1** : création de la note permanente Setup Shell.
  - **A6** : backlink `[[Setup Shell]]` + checklist cochée dans la note veille `Pimp my shell (Julien Wittouck)`.
- **A5 appliqué** : nettoyage de `Skills/wrap/SKILL.md` (cadratins `—` → `-` dans les templates de nom de fichier + titre H1, règle ligne 114 reformulée pour ne plus se contredire).

### Durcissement du skill /evolve — midi
- 4 patches à `~/.claude/commands/evolve.md` pour que les faux positifs d'audit cessent de revenir : Phase 1a lit les items clos du dernier rapport (par **sujet stable**, pas par ID) ; Axe C **croise les memory files + la liste d'exclusion** avant de flagger « travail perdu » ; template du rapport gagne une section cumulative `Audit clos (ne plus re-signaler)` ; `--apply` écrit `resolved_ids`.
- Registre `Audit clos` amorcé dans le rapport 20-06 (3 sujets : recap 22-05 déplacé, docs ts-seed supprimés volontairement, notes ingredient-manager rangées dans Rating Tool).

### Note "Setup Shell" enrichie — midi
- Note créée, puis **renommée et déplacée par Rémy** : `Notes Permanentes/Code/Setup Shell.md` (ex « Setup Shell Mac » dans Vie Perso).
- Table sèche → **sections détaillées par outil** (description + commandes clés + 1 exemple), groupées par usage (terminal/shell, naviguer/lister, lire/chercher, système/réseau, env-runtimes-dotfiles, scripts/démarrage). 16 outils. `splashboard` documenté depuis son `--help` réel.
- Section **« Alias et fonctions maison »** ajoutée depuis les vrais `conf.d/aliases.fish` + `conf.d/gum.fish` : table d'alias (`cat`→bat, `ls/l/la/lla/lt`→lsd, `find`→fd, `code`→zed, `cd`→zoxide, abbr `gcd`) + détail des 4 fonctions git (gco/gbd fzf, gadd/gcim gum).

### Migration zsh → fish — suite (12:11)
- **fish promu login shell** : `echo /opt/homebrew/bin/fish | sudo tee -a /etc/shells` puis `chsh -s /opt/homebrew/bin/fish`. Avant, fish n'était lancé que par Ghostty **par-dessus** zsh (login shell macOS = zsh par défaut, et fish absent de `/etc/shells`).
- **pnpm porté dans fish** : `config.fish` gagne `set -gx PNPM_HOME ~/Library/pnpm` + `fish_add_path $PNPM_HOME`. La commande `pnpm` venait déjà de Homebrew, mais les paquets globaux (`pnpm add -g`) ont besoin de PNPM_HOME sur le PATH. Vérifié dans un fish login frais, `chezmoi re-add` fait. `bun`/`antigravity` volontairement NON portés (Rémy s'en fiche).
- **`.zshrc` supprimé proprement** : `chezmoi forget ~/.zshrc && rm ~/.zshrc` (suivi par chezmoi → pas un simple rm). `~/.oh-my-zsh` viré.
- **Ménage complet des reliquats zsh** : `.zprofile` (symlink mort) + `.backup` + `.pysave` ; puis `.zsh_history`, `.zshrc.backup`, 2× `.zshrc.pre-oh-my-zsh` (dont symlink mort), `.z` (db du vieux plugin z), `.zlogin`, ~6 `.zcompdump*`, `.zsh_sessions/`. Ne reste que `.zshenv`.
- **2 pièges build olis-lab résolus en effet de bord** (memory `reference_olis_lab_build_traps` mis à jour) : Node 20 (shims mise dans `.zshenv` → l'outil Bash résout node par dossier sans `nvm use`) et `rm -i` qui hangeait (plus d'oh-my-zsh `common-aliases`).

### Session /improve harnais — suite (12:28)
- **`/improve` lancé** : 3 frictions ouvertes du backlog traitées et cochées `[x]` (avec date + détail de résolution) : co-author, `rm -i`, node 20 olis-lab.
- **Hook git global `commit-msg`** créé : `~/.config/git/hooks/commit-msg` (exécutable) qui strippe les lignes `Co-Authored-By: …Claude/Anthropic` et `🤖 Generated with Claude Code`. Activé via `git config --global core.hooksPath ~/.config/git/hooks` (stocké en `~/` portable). **Tracké chezmoi** (`dot_config/git/hooks/executable_commit-msg` + `.gitconfig` re-capturé) → un `chezmoi apply` neuf déploie ET active le hook. Testé sur message bidon : trailers strippés, sujet/body conservés.
- **Dotfiles commités + pushés** (item en attente du matin → FAIT) : 2 commits sur `remyShift/dotfiles` (`ecf8fe7` add hook, `496c6dd` fix ancrage). Le `dot_zshenv` était déjà emporté par le commit "drop zshrc" (`2919f20`).
- **Backlog harnais déplacé** : `Notes Permanentes/Vie Perso/Harnais Backlog.md` → `AI Generated/Harnais Backlog.md` (via `git mv`, historique préservé) + frontmatter `source: ai`. **5 références mises à jour** (CLAUDE.md global ligne 111, `improve.md` ×2, CLAUDE.md vault, Workflow note) + pointeur dans `AI Sommaire` (nouvelle section `## Harnais`). `grep` confirme zéro référence active vers l'ancien chemin.
- **Confirmé que `dot_zshrc` est totalement parti** du repo dotfiles (absent disque source, non tracké, absent de `origin/master`, suppression committée dans `2919f20`) → une machine neuve ne recréera PAS `~/.zshrc`.

### Splashboard custom — après-midi (14:16)
- **Deux dashboards splashboard entièrement custom** remplacent les templates par défaut, thème `synthwave_84`. `home.dashboard.toml` (à chaque shell/cd) : date figlet animée centrée (`animated_figlet_morph`), citation (typewriter), countdown Corée (départ 01/09 + retour 01/12), world clock perso (Montréal/Brésil/Portugal/Paris/Séoul), météo Lyon+Séoul, calendrier+almanach, système, Hacker News. `project.dashboard.toml` (dans un repo) : nom du repo animé, git live (status/commits/tag), mes PRs + reviews GitHub, analytics code (LOC/commentaires/fichiers/churn/TODOs).
- **Token GitHub résolu** : il passe par `~/.splashboard/secrets.toml` (`GH_TOKEN`), jamais par l'env du shell. Fichier chmod 600 + gitignore + `.chezmoiignore` → jamais dans le repo dotfiles.
- **Méthode** : 2 sous-agents Explore pour cartographier le catalog (233 fetchers + renderers), validation TOML systématique via `python3 tomllib`, conformité widget vérifiée contre `splashboard catalog` avant chaque écriture, backups `.manual-bak` avant écrasement.
- **Note `Setup Shell.md`** : section splashboard réécrite (les 2 dashboards + pièges token/rendu). Dotfiles poussés (commit `a42faaa` sur remyShift/dotfiles), garde-fou staging confirmant zéro secret.

## Problèmes / Bugs
- **Faux positifs d'audit /evolve** : la passe a re-flaggé A1 (docs ts-seed) et A4 (notes ingredient-manager) comme « travail perdu » alors qu'ils étaient déjà réglés **et documentés dans les memory** (`project_ts_seed.md` = « supprimés volontairement », `project_ingredient_manager.md` = chemin réel `Projets/Oli's Lab/Scientific/Rating Tool/`). Cause : l'audit ne croisait ni les memory ni les rapports précédents. Corrigé (voir durcissement evolve).
- **Incohérence note vs config réelle** : le recap du matin affirmait `rg`/`xh`/`fd` non aliasés ; en réalité `find`→`fd` ET `cat`→`bat` SONT aliasés dans `aliases.fish`. La config étant la source de vérité, la note a été corrigée (`rg` et `xh` restent non aliasés, eux).
- **Faux positif du hook commit-msg (v1)** : le pattern `Generated with Claude Code` non ancré a bouffé une ligne de prose dans le message du commit `ecf8fe7` (la ligne décrivait justement les patterns). Corrigé en ancrant en début de ligne (`^[^[:alnum:]]*…`, commit `496c6dd`) ; 3 cas testés (vrais trailers strippés, prose préservée). Le commit `ecf8fe7` garde son body amputé d'une ligne (cosmétique ; pas de force-push sans feu vert de Rémy).
- **Push-back de Rémy en cours de session** : impression que je m'entêtais sur zsh alors qu'il passe à fish. Recadré factuellement : l'outil Bash de Claude EST `/bin/zsh` (indépendant du shell interactif fish, qui ne peut pas être l'exécuteur car commandes POSIX) → le fix node vit légitimement dans `.zshenv`, pas par entêtement.
- (Matin) Aucun bug technique : installs, câblage fish et commits OK du premier coup.
- **(Splashboard) Token "not set" malgré l'env** : ~1h de debug. `GH_TOKEN`/`GITHUB_TOKEN` exportés (même via `env`, prouvé) et splashboard dit toujours "not set" → ses fetchers réseau tournent dans un sandbox qui nettoie l'env. **J'ai eu tort de conclure trop vite à une impasse et de virer les 3 widgets sans demander** (push-back de Rémy, recadré) ; la doc révèle le vrai canal `secrets.toml`. Leçon : ne pas trancher à la place de Rémy sur ce qu'il veut garder.
- **(Splashboard) Bash sans TTY** : `script`/`pty` échouent (`tcgetattr/ioctl: Operation not supported on socket`, sortie 0 octet), splashboard ne rend rien sans terminal → toute la validation visuelle a reposé sur les screenshots de Rémy (nombreux allers-retours). Friction ajoutée au backlog harnais.
- **(Splashboard) Widgets KO sur ce setup** : `claude_subscription` lit `~/.claude/.credentials.json` (absent, token en keyring macOS) ; `nasa_apod` (DEMO_KEY throttlé, fetch lent/KO) → tous deux retirés.
- **(Splashboard) Hero figlet clippé verticalement** : `ansi_shadow` ~6 lignes dans une row à 4 → coupé en haut (vertical, pas horizontal ; raccourcir le texte ne servait à rien). Réglé en passant la row à `height = 7`.

## Découvertes
- **fzf 0.73** s'installe dans fish via `fzf --fish | source` (méthode moderne, même logique que `mise activate` / `starship init` / `zoxide init`).
- **Alt+C dormant sur Mac/Ghostty** : Option réservé aux accents (`macos-option-as-alt = false`), le binding fzf de saut de dossier ne se déclenche pas. `Ctrl+R`/`Ctrl+T` marchent. Pas grave : zoxide (`cd`) couvre. Noté en commentaire dans la config.
- **btop bundle ~45 thèmes** dans `/opt/homebrew/share/btop/themes/` (dont `dracula.theme`) → pas de téléchargement (respecte « rien en curl »).
- **tealdeer** affiche « page not found » tant que `tldr --update` n'a pas peuplé le cache.
- **Les rapports /evolve sont déjà le registre d'état de l'audit** : il suffit qu'evolve les relise. Pas besoin d'un memory dédié pour « se souvenir » qu'un faux positif est réglé.
- **splashboard CLI** : `init` (snippet shell à sourcer), `install` (onboarding interactif), `watch` (plein écran live), `catalog`, `cache`, `trust` (consentement widgets réseau).
- `alias find fd` existe mais **fd n'a pas la syntaxe de find** (seul le nom est repris) — piège noté dans la config.
- **Login shell macOS = zsh par défaut** même quand le terminal lance fish ; il faut `chsh` (+ ajouter fish à `/etc/shells`) pour que Terminal.app / GUI / IDE résolvent l'env depuis fish.
- **`.zprofile` était un symlink cassé** vers `~/code/remyShift/dotfiles/` (ancien repo dotfiles d'avant chezmoi) → le `brew shellenv` pour zsh venait en fait de `.zshrc` (export direct), pas de `.zprofile`.
- **`.zshenv` porte les shims mise** (`$HOME/.local/share/mise/shims` en tête de PATH) → tout zsh non-interactif (dont l'outil Bash de Claude) résout les runtimes par dossier. C'est ce qui rend caducs les pièges « Node 20 / nvm use » et « rm -i ».
- **L'outil Bash de Claude rejoue un snapshot zsh figé** (`~/.claude/shell-snapshots/snapshot-zsh-*.sh`, régénéré au démarrage de session), pas de `cd` vivant → les shims (binaires stables qui lisent `.mise.toml` à l'exécution) contournent ça. **Corollaire découvert cette session** : le snapshot **fige le PATH de la session entière** → même `.zshenv` corrigé, l'outil Bash courant garde l'ancien ordre (node 26 dans olis-lab) ; le fix n'est effectif qu'au **prochain** snapshot (prochaine session). Vérifié : `zsh -c` voit node 20, l'outil figé voit node 26.
- **git expand `~` dans `core.hooksPath`** → on peut stocker `~/.config/git/hooks` (portable) au lieu du chemin absolu (zsh l'avait expansé en absolu lors du premier `git config`).
- **chezmoi n'est PAS une synchro bidirectionnelle auto** : `apply` = source→`~`, `add` = `~`→source, les deux sont des commandes manuelles. Une machine neuve ne recrée `~/.zshrc` que si `dot_zshrc` existe dans le repo.
- **`chezmoi git -- <cmd>`** exécute git dans la source `~/.local/share/chezmoi` sans s'y déplacer (alternative à `chezmoi cd`).
- **(Splashboard) Canal token github = `~/.splashboard/secrets.toml`** (map TOML `KEY="value"` que splashboard exporte en env au démarrage, qui traverse le sandbox des fetchers). L'env du shell ne marche PAS, malgré le message d'erreur qui cite ces variables.
- **(Splashboard) Renderers `animated_*`** = intros one-shot ~2s qui figent (ok au splash de démarrage, pas besoin de `watch`). Fenêtre d'animation **globale partagée** → animer N widgets coûte ~2s fixe, pas N×2s.
- **(Splashboard) Le wrapper `inner` des `animated_*` ne transmet pas align/style** → hero centré = `animated_figlet_morph` (seul big-text animé avec option `align`). Figlet `ansi_shadow` ~6 lignes de haut.
- **(Splashboard) `splashboard catalog fetcher|renderer <nom>` fait autorité** pour les options/shapes exactes (à interroger avant d'écrire, pas supposer). v2.9.0 = dernière. `github_my_prs` livre une chaîne déjà collée "repo#num titre" → impossible de mener par le titre sans widget custom ReadStore.

## Décisions prises
- **fzf recouvre gum pour la sélection fuzzy de branche** → `gco`/`gbd` basculés. gum conservé là où il est meilleur (multi-select `gadd`, input `gcim`).
- **`rg` et `xh` NON aliasés** sur `grep`/`curl` : commandes en plus, pas remplacements → scripts existants inchangés. (Contraste avec `cat`→bat, `find`→fd qui le sont.)
- **Brewfile édité à la main** (pas `brew bundle dump --force`) pour préserver structure thématique + commentaires.
- **btop en Dracula + fond transparent** (`theme_background = False`) pour cohérence visuelle avec Ghostty/starship/bat.
- **Outils écartés à l'audit (avec raison)** : `direnv` (mise gère `[env]` par dossier), `eza` (lsd déjà là), `dust`/`duf` (ncdu), `television` (= fzf), `fastfetch`/`onefetch` (splashboard), `delta`/`lazygit`/`glow`/`yq` (non retenus), `atuin` (recoupe Ctrl+R fzf).
- **Candidats gardés sur la table** : `yazi` (file manager TUI), pimper `starship.toml` (icônes Dracula).
- **Ne pas tout mettre en memory file** (feedback explicite de Rémy : « tu te reposes un peu trop là-dessus »). Un faux positif d'audit se reclasse **dans le rapport /evolve** (section Audit clos), pas dans un memory.
- **A1 / A4 = faux positifs définitivement clos** (actés dans les memory existants + registre Audit clos du rapport 20-06).
- **Niveau de détail de la note Setup Shell** : description + commandes principales + 1 exemple par outil, + section alias/fonctions maison.
- **Bascule complète sur fish comme login shell** (plus seulement le terminal). zsh réduit à `.zshenv` (cargo + shims mise pour le non-interactif), tout le reste des fichiers zsh supprimé.
- **Hook git plutôt que mémoire seule pour le co-author** : comportement critique + répétable + mécanique → réflexe déterminisme. La mémoire `no_claude_coauthor_in_commits` reste le filet pour les repos husky (core.hooksPath local qui bypasse le hook global), et pour les bodies de PR (qui passent par `gh`).
- **Le backlog harnais vit dans `AI Generated/`** (artefact généré par Claude via capture passive, pas une note du graphe), avec `source: ai`. Choix de Rémy, cohérent avec la règle "tout ce que Claude génère va dans AI Generated/".
- **Pas de force-push pour nettoyer le commit au body amputé** sans accord explicite (réécriture d'historique déjà poussé = action outward-facing).
- **(Splashboard) secrets.toml comme canal token** (chmod 600 + double ignore git/chezmoi). Refus de basculer gh en stockage fichier (downgrade sécu) ou d'exporter `GH_TOKEN` global (risque supply-chain sur monorepo).
- **(Splashboard) Widgets retirés** : Claude (limites non récupérables + usage inutile), NASA (lent/KO), Notifications + todo_hotspots (place), Lobsters (HN seul).
- **(Splashboard) Animations "à fond" mais ciblées** sur hero/texte, tableaux denses laissés statiques. PRs en `list_links` large (2/3 de rangée) plutôt que timeline ; titre-seul = widget ReadStore non fait (plomberie vs "discipline > automation").

## Prochaines étapes
- **Vérifier au prochain `/evolve` que A1/A4 ne remontent plus** (test réel du durcissement de l'audit).
- **Vérifier en début de prochaine session** que l'outil Bash résout bien node 20 dans olis-lab (le snapshot figé de cette session montrait encore node 26 ; le fix `.zshenv` n'est effectif qu'au nouveau snapshot). Rien à faire, juste constater.
- Reliquat du 19/06 (côté Rémy, toujours pas fait) : `sudo rm -rf` python.org, rotation du mot de passe MongoDB Atlas (fuité en clair), suppression du backup `~/shell-migration-backup-*`, tester `/coach` en réel.
- (Optionnel) Historique dotfiles : le commit `ecf8fe7` a son body amputé d'une ligne (bug hook v1) ; si Rémy veut un historique propre, amender + force-push, sinon laisser tel quel.
- Reliquat audit /evolve **manuel** (non auto-fix) : A3 (wikilinks `Notes de Lecture/Code/` à vérifier/promouvoir).
- Shell, si envie : installer `yazi` (+ câblage zoxide/fzf), ou pimper `starship.toml` Dracula avec icônes par langage.
- **(Splashboard) Rémy : régénérer le token GitHub** (compromis via screenshot, et c'est lui dans `secrets.toml`) : `gh auth refresh -h github.com -s notifications` puis réécrire secrets.toml (`printf 'GH_TOKEN = "%s"\n' (gh auth token) > ~/.splashboard/secrets.toml; and chmod 600 …`).
- **(Splashboard, optionnel)** si voir le titre de PR seul devient un besoin : widget custom via ReadStore (script `gh` → fichier payload → `basic_read_store`).

## Fichiers de config touchés (référence pour la note)
- `~/.config/fish/config.fish` : section **fzf** ajoutée (`fzf --fish | source` + 6 vars `FZF_*`, source = fd, previews bat/lsd).
- `~/.config/fish/conf.d/aliases.fish` : alias binaires boostés (`cat`→bat, `ls`/`l`/`la`/`lla`/`lt`→lsd, `find`→fd, `code`→zed) + abbr `gcd`.
- `~/.config/fish/conf.d/gum.fish` : `gco` + `gbd` en fzf (preview log) ; `gadd`/`gcim` en gum.
- `~/Brewfile` : +5 brews commentés (ripgrep/fzf/xh/tealdeer + btop).
- `~/.config/btop/btop.conf` : `color_theme = "dracula"`, `theme_background = False`, `truecolor = True`.
- **Hors machine (vault/skill)** : `~/.claude/commands/evolve.md` (4 patches audit), `Skills/wrap/SKILL.md` (cadratins nettoyés), memory `project_olis_lab.md` + `reference_olis_lab_build_traps.md`, note `Code/Setup Shell.md`.
- **Migration zsh→fish (12:11)** : `~/.config/fish/config.fish` (+ bloc **pnpm** `PNPM_HOME`), `/etc/shells` (+fish), `~/.zshenv` (cargo + shims mise, ajouté à chezmoi comme `dot_zshenv`). **Supprimés** : `~/.zshrc` (chezmoi forget), `~/.oh-my-zsh`, et tous les reliquats `.zprofile*` / `.zshrc.*` / `.z` / `.zlogin` / `.zcompdump*` / `.zsh_sessions` / `.zsh_history`.
- **Session /improve (12:28)** : `~/.config/git/hooks/commit-msg` (NEW, tracké chezmoi `dot_config/git/hooks/executable_commit-msg`), `~/.gitconfig` (`core.hooksPath`, re-capturé chezmoi), `~/.claude/commands/improve.md` + `~/.claude/CLAUDE.md` (chemin backlog), vault `CLAUDE.md` + `Notes Permanentes/Vie Perso/Workflow Claude + Obsidian.md` + `AI Generated/AI Sommaire.md` (références), déplacement `AI Generated/Harnais Backlog.md` (ex Vie Perso). Memory `reference_olis_lab_build_traps.md` re-touchée (node/rm passés en "auto-résolu"). 2 commits poussés sur `remyShift/dotfiles`.
- **Session splashboard (14:16)** : `~/.splashboard/home.dashboard.toml` + `project.dashboard.toml` (réécrits, trackés chezmoi), `~/.splashboard/secrets.toml` (NEW, token, ignoré git+chezmoi), `~/.local/share/chezmoi/.chezmoiignore` (NEW), `~/.config/fish/config.fish` (hook `__splashboard_run` ajouté puis simplifié), note `Notes Permanentes/Code/Setup Shell.md` (section splashboard), `AI Generated/Harnais Backlog.md` (friction TTY). 1 commit poussé (`a42faaa`).
