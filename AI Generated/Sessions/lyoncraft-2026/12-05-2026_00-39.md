---
date: 12-05-2026
heure: 00:39
project: lyoncraft-2026
source: ai
tags: [session]
---

# Recap — lyoncraft-2026 — 12/05/2026

## Avancées
- Système de theming dark/light complet mis en place sur la presentation Slidev
- Toutes les couleurs hardcodees extraites vers des CSS variables dans `styles/index.css`
- Variables renommees de `-dark` vers `-bg` pour plus de clarté semantique
- Bloc `html:not(.dark)` ajoute avec les valeurs light pour chaque variable
- Nouvelles variables ajoutees : `--color-blue-glow`, `--color-surface-overlay`, `--overlay-bg`, `--cover-gradient-accent`, `--text-context`
- `color-mix()` utilise pour les valeurs rgba derivees dans les composants (shadows, overlays)
- Tous les composants mis a jour : `Tag.vue`, `TimelineItem.vue`, `SkillCompare.vue`, `SequenceBlock.vue`, `Door.vue`, `SlideTop.vue`
- `colorSchema: dark` → `colorSchema: auto` dans `slides.md` pour activer le toggle natif Slidev
- Peaufinage manuel des valeurs light par Remy (fond, overlays, couleurs de contexte)

## Problemes / Bugs
- Aucun bug bloquant rencontre

## Decouvertes
- Slidev gere nativement le toggle dark/light via `colorSchema: auto` — bouton soleil/lune dans la barre de navigation, pas de code custom necessaire
- La classe `dark` est ajoutee sur `html` par Slidev/Vite, ce qui permet d'utiliser `html:not(.dark)` pour le theme light
- `color-mix(in srgb, var(--color) 30%, transparent)` est une alternative propre aux rgba hardcodes — suit automatiquement la valeur de la CSS var
- Les styles `scoped` dans les composants Vue ont precedence sur les styles globaux de `index.css` pour les memes classes (ex: `.tag-green` dans `Tag.vue` ecrasait les regles globales)

## Decisions prises
- Variables suffixe `-bg` plutot que `-dark` pour les fonds de badges/tags (semantique plus claire)
- Couleur cyan `#01ccff` / `#0177ff` du `SkillCompare` maintenue comme `--text-context` — c'est une couleur de design intentionnelle, pas une couleur de theme generique
- `color-mix()` utilise au lieu de variables rgba supplementaires — moins de variables, meme resultat
- Les couleurs de prop dynamiques de `Door.vue` (passees par slides) restent hardcodees dans les slides — elles sont des couleurs de contenu, pas de theme

## Prochaines etapes
- Remplacer placeholder KicksFolio (slide 19) par vraies screenshots
- Remplacer `{PLACEHOLDER_POST_LINKEDIN}` (slide 22) par le vrai texte du post
- Verifier le rendu visuel complet en light ET dark mode sur toutes les slides
- Chronometrer chaque acte (objectif : 45 min total)
- Decider si slide "Splash" dans l'Acte 3
