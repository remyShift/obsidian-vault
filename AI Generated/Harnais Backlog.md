Backlog des frictions du harnais Claude (CLAUDE.md, skills, commandes, hooks, structure du vault). Alimenté **automatiquement** en fin de session par la capture passive (règle dans le CLAUDE.md du vault), et traité manuellement par `/improve`.

Format des entrées (append-only, ne pas réécrire les lignes existantes) :

`- [ ] [observation|explicite] description courte - DD-MM-YYYY`

- `[observation]` : friction détectée seule par Claude en fin de session.
- `[explicite]` : idée ajoutée à la main (via `/improve ajouter <description>`).

Une fois une amélioration appliquée par `/improve`, sa ligne est cochée `[x]` avec la date, jamais supprimée (registre de suivi).

---

## Frictions

<!-- les entrées s'ajoutent ici, plus récent en bas -->
- [x] [observation] Sous-agent Explore a affirmé avec aplomb l'existence de fichiers (read CRA) en réalité absents du working tree local (16 commits de retard) ; vérité obtenue seulement en vérifiant `origin/develop`. Penser à instruire les sous-agents Explore de vérifier l'état git (local vs origin) quand le contexte mentionne un merge récent. - 15-06-2026 ✅ 28-06-2026 : garde-fou état git ajouté à la section "Délégation aux sous-agents" du CLAUDE.md global (vérifier `git status`/`origin` avant d'affirmer qu'un fichier existe quand le contexte mentionne un merge récent).
- [ ] [observation] L'outil Edit refuse d'éditer un fichier déjà lu via `Bash cat`/`git show` et exige un `Read` préalable — re-lecture redondante (SiteHeader, hot.md cette session). - 15-06-2026
- [x] [friction] Chaque appel Bash dans olis-lab doit re-sourcer nvm + `nvm use 20.19.6` (état shell non persistant entre tool calls) - répété 10+ fois - 16-06-2026 ✅ 20-06-2026 : nvm périmé (migré mise). Réglé en mettant les shims mise en tête de PATH dans `~/.zshenv` (tracké chezmoi). L'outil Bash (zsh non-interactif) lit `.mise.toml` au runtime via les shims → node 20 auto dans olis-lab, sans `cd` vivant. Effectif dès la prochaine session : l'outil Bash rejoue un snapshot zsh figé (`~/.claude/shell-snapshots/snapshot-zsh-*.sh`) régénéré au démarrage.
- [ ] [friction] ExitPlanMode a échoué une fois sur "Tool permission stream closed before response received" (glitch harnais) - 16-06-2026
- [ ] [observation] Écrire un escape unicode `̀-ͯ` dans une regex via Write/Edit : mon input rend les caractères combinants littéraux au lieu de la séquence ASCII `\u…`, 3 tentatives + workaround `printf` shell nécessaires - 17-06-2026
- [x] [observation] `rm` aliasé `rm -i` (plugin omz common-aliases) fait hanger l'outil Bash : le shell non-interactif source `.zshrc`, `rm` attend une confirmation et bloque (passé en background, hang) ; contourné via `/bin/rm` (2 occurrences cette session) → envisager `unalias rm` / `command rm` par défaut. - 19-06-2026 ✅ 20-06-2026 : réglé par la suppression de `.zshrc` (migration full fish). Plus d'omz common-aliases sourcé par l'outil Bash zsh → plus d'alias `rm -i` qui hangait.
- [ ] [observation] Le classifieur rejette TOUTE la commande Bash composée quand une seule sous-action est interdite (ex: vidage `/var/mail`) → perte des étapes sûres de la même commande ; réflexe à prendre : isoler les actions à risque dans des appels Bash séparés. - 19-06-2026
- [x] [observation] L'instruction harnais par défaut ajoute `Co-Authored-By: Claude` aux commits malgré la préférence "jamais" de Rémy (mistake répété cette session) ; mémoire créée, mais un réglage/hook qui strippe le trailer serait plus robuste. - 19-06-2026 ✅ 20-06-2026 : hook git global `commit-msg` installé (`~/.config/git/hooks/commit-msg` + `git config --global core.hooksPath ~/.config/git/hooks`) qui strippe les lignes `Co-Authored-By: …Claude/Anthropic` et `Generated with Claude Code`. Testé OK sur un message bidon. Limite : un repo avec `core.hooksPath` local (husky) bypasse le hook → la mémoire `no_claude_coauthor_in_commits` reste le filet. Ne touche pas les bodies de PR (passent par `gh`).
- [ ] [observation] zsh expand les globs AVANT de passer la commande → `grep --include=*.ts` et `ls tsconfig*.json` échouent en "no matches found" (2 occurrences cette session) ; réflexe : quoter les patterns (`--include='*.ts'`) ou utiliser des chemins explicites. - 19-06-2026
- [ ] [observation] L'outil Bash rejoue un snapshot zsh figé capturé au démarrage de session (`~/.claude/shell-snapshots/snapshot-zsh-*.sh`, PATH en dur) : un fix de config shell (`.zshenv`, ordre du PATH) n'est PAS vérifiable dans la même session, et il a fallu de nombreux round-trips avant de comprendre que node restait en 26 dans olis-lab à cause du snapshot, pas du fix. Réflexe : pour valider un changement de dotfile zsh, tester via `zsh -c 'source …; …'` plutôt que de se fier à l'état de l'outil Bash courant. - 20-06-2026
- [ ] [observation] L'outil Bash n'a pas de vrai TTY (`script`/`pty.spawn` échouent : `tcgetattr/ioctl: Operation not supported on socket`, sortie 0 octet) → impossible de rendre/tester un programme qui ne dessine que sur terminal (splashboard, TUI) ; toute la validation visuelle a dû passer par les screenshots de Rémy = nombreux allers-retours sur la session splashboard. Réflexe : pour un outil TTY-only, annoncer d'emblée que le rendu se vérifie côté user et concentrer mes tests sur le vérifiable (parse, conformité catalog, logs). - 20-06-2026
- [x] [observation] `vault:search_files` ne recurse PAS avec un glob simple `*X*` (cherche à la racine seulement) → 8 recherches à vide avant de comprendre qu'il faut préfixer `**/` (`**/*Wittouck*`). Réflexe : toujours préfixer les patterns de `vault:search_files` par `**/`. - 21-06-2026 ✅ 28-06-2026 : règle dure ajoutée en "Pièges outils" dans le CLAUDE.md du vault.
- [x] [observation] Pas d'outil de recherche par CONTENU ni de recherche sémantique/RAG exposé côté MCP vault : seulement recherche par nom de fichier (`search_files`) + lecture. Trouver une note par sujet quand on ignore son nom de fichier oblige à deviner le nom et multiplier les essais. Un `ctx_search`/grep sur le contenu manque à l'appel. - 21-06-2026 ✅ 28-06-2026 : obsolète, le MCP `vault-rag` (recherche sémantique sur tout le vault) existe et est documenté dans le CLAUDE.md (section "Recherche sémantique (RAG)").
- [ ] [observation] Le hook global `commit-msg` qui strippe `Co-Authored-By` (installé 20-06, cf. ligne du 19-06) est **bypassé par les repos à `core.hooksPath` local (husky)** : seed4t pointe sur `.husky/_`, donc le trailer Claude est repassé cette session → nettoyage manuel (réécriture `77ae7ec` + force-push avec ruleset désactivé) coûteux. Pistes /improve : faire stripper le trailer aussi par le hook husky `commit-msg` du repo (chaîner le global), ou un check pre-push. Filet ajouté ce jour : règle "jamais de co-signature" dans le CLAUDE.md global. - 23-06-2026
- [ ] [observation] Lancer un script one-shot avec contexte Payload (`getPayload` + `@payload-config`) via `payload run` : le script DOIT être en **top-level await** (sinon le runner rend la main et le process sort avant la fin de l'async → exit 0 totalement muet), et `process.exit()` coupe stdout (logger pino async) → écrire le résultat via `fs.writeFileSync` pour le voir. ~5 allers-retours avant de comprendre (j'ai d'abord cru à un souci d'env, puis de slug). Réflexe : tout script payload-run de seed/vérif = top-level await + `fs.writeFileSync` pour la sortie. - 23-06-2026
- [x] [observation] La commande /recap injecte une date/heure hardcodée stale ("17-06-2026 17:53") dans ses instructions au lieu de la date réelle (23-06) → réflexe pris : lire `date` moi-même. À corriger côté template /recap pour qu'il dérive la date à l'exécution. - 23-06-2026 ✅ 28-06-2026 : déjà réglé. La version actuelle du skill recap dérive la date à l'exécution via un bloc `date "+%d-%m-%Y %H:%M"` (étape 3). Rien à corriger.
- [ ] [observation] Crash de session pendant un gros chantier destructif (suppression legacy checkout) : tout le working tree a été reverté à HEAD (90+ edits + suppressions `git rm` perdus), seule la branche a survécu → refait l'intégralité depuis le plan. Réflexe : sur un chantier de suppression massive, committer par incréments tôt plutôt que d'accumuler 95 fichiers non commités. À voir /improve : un autosave/stash périodique du working tree côté harnais limiterait la perte. - 23-06-2026
- [ ] [observation] Le classifier de sécurité Bash a été indisponible par intermittence (`claude-opus-4-8 temporarily unavailable, auto mode cannot determine safety`) pendant plusieurs minutes, bloquant même des scripts python read-only / dry-run → bascule forcée sur l'outil Edit pour les modifs ne passant pas par Bash, et attente pour le reste (install, git). Réflexe : quand le classifier tombe, basculer le maximum de travail sur Read/Edit/Write et garder les commandes Bash en file pour quand il revient. - 23-06-2026
- [x] [observation] L'outil str_replace/Edit natif opère sur le FS du conteneur, pas sur le FS du MCP vault → échec "File not found" en tentant d'éditer Sommaire.md via str_replace, fallback réécriture complète via vault:write_file. Réflexe : pour éditer une note du vault, passer par vault:edit_file (édition par lignes) ou vault:write_file, jamais str_replace/Edit natif. - 23-06-2026 ✅ 28-06-2026 : règle dure ajoutée en "Pièges outils" dans le CLAUDE.md du vault (avec le piège `**/` de search_files).
- [ ] [observation] L'outil Bash tournait sous Node 26 dans olis-lab (repo pin 20.19) malgré le fix mise-shims-in-PATH du 20-06, ET `mise exec -- pnpm …` tombe sur un node Homebrew 25 cassé (dyld `libllhttp.9.3.dylib` not loaded) → `pnpm build` / `generate:importmap` impossibles en local, importMap édité à la main en fallback. Réflexe : vérifier `node -v` avant toute commande qui exige le node du repo, et déléguer la génération à Rémy si l'env est cassé. - 25-06-2026
- [ ] [observation] Le shell de l'outil Bash (fish) expand les globs avant la commande et échoue en "no matches found" sur les chemins pnpm contenant `@`/`+`/`[]` (`node_modules/.pnpm/@payloadcms+ui@*/…`) → plusieurs ls/grep à vide, contournés via `find` ou `node -e require.resolve`. Réflexe : pour fouiller node_modules pnpm, utiliser `find` / `require.resolve`, pas des globs. - 25-06-2026
