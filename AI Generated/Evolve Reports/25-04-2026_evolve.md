---
date: 25-04-2026
type: evolve-report
status: pending-review
tags: [evolve, meta]
---

# Evolve Report — 25/04/2026

**Sources analysées** :
- 8 recaps (olis-lab : 20, 21, 24/04 — obsidian-vault : 18, 20/04 — portfolio-game-boy-next : 23, 24, 25/04)
- 5 conversations (18, 19, 21x2, 24/04)
- **1 meeting** (Tech Weekly olis-lab 25/04 — première intégration des notes meetings dans /evolve)

**Nouveau projet détecté** : `portfolio-game-boy-next` (3 sessions sur 3 jours, jamais documenté en memory)

**Notes Permanentes cartographiées** : ~120 fichiers, 50+ dossiers
**Memory Claude** : 2 projets (obsidian-vault : 8 fichiers, olis-lab : 1 MEMORY.md)
**Suggestions** : 9 total — 4 memory, 3 vault enrichissement, 2 audit

> ⚠️ **Note importante** : le Tech Weekly du 25/04 a corrigé une inférence majeure faite à partir des recaps Claude seuls. Les recaps olis-lab du 21/04 portaient sur la **préparation de la session Diego** (audits + décisions migration Merchant API), ce qui m'a fait croire que c'était le chantier principal. Le meeting confirme que **le chantier quotidien réel est la migration CMS Payload** (brands, products, bridge script). La migration Merchant API est un workstream parallèle long terme. Le memory `project_olis_lab.md` est corrigé en conséquence dans M1.

---

## Partie 1 — Memory Claude

### [M1] update — `project_olis_lab.md`

**Projet** : obsidian-vault
**Pattern** : 3 recaps olis-lab (20, 21, 24/04) + Tech Weekly 25/04. Les chantiers actifs du 17/04 (Tests Vitest + i18n SkinTypes) sont absorbés ; le tableau de bord réel est plus large et plus net dans le meeting que dans les recaps.

**Modification** — remplacer la section "Chantiers actifs (17/04)" par :

```
**Chantiers actifs (25/04, source Tech Weekly)** :

Chantier principal — **Migration CMS Payload** :
- PRs en attente de merge : brands collection, bridge script, products collection, schema associé
- Refactoring seed script (Rémy) — fait, retrait du champ images en attente de clarification media collection
- Media management bloquée sur définition tailles d'images — réunion à planifier (Rémy + Michele + Diego)
- Bridge script abstraction discutée (infra commune + mapping custom par collection). Cas particulier : categories/subcategories (structure atypique legacy → script séparé probable)
- Tests CMS exploratoires Payload (admin UI, hooks, validation slug)
- La migration de la **product display page** vers Next.js dépend du merge des PRs products + media → priorité haute

Workstream parallèle long terme — **Migration Content API → Merchant API** (sunset 18/08/2026) :
- 5 documents stratégiques rédigés pour session Diego (recaps 21/04)
- Recommandation Option B (XML feed + DataSources S3/CloudFront) au lieu de push 1:1
- Sandbox plan rédigé (variante A : data source `[STAGE]` avec `excluded_destinations`)
- Pas un chantier quotidien — déclenché après arbitrage Diego

Chantier critique transverse — **Event tracking core user journey** :
- PostHog + GA + Meta doivent fonctionner simultanément (deadline fin avril)
- Apple Pay bug : GA session ID/client ID pas passés au serveur en cookies (Michele + Kyle)
- Checkout v2 bloqué tant que les events ne passent pas correctement
- À terme : service centralisé back-end qui dispatche aux providers

Autres chantiers en cours :
- Feature **Scoring Ingredient List** (Patrick) : matching back/front + scoring logic, deadline mercredi prochain (Diego absent)
- Refactoring Product Recommendation (Patrick + Diego)
- Performance Shop All + cleanup assets publics + tâche SEO meta (Lucas)
- Stock / Bigblue : Option 3 (DB cache + webhook + reconciliation), fix HMAC `bigBlue.js:26-29` prérequis non négociable

Bugs résiduels GMC non bloquants : typo `content`/`apiContent` (`googleMerchantUtils.js:210`), `updateProductAvailability` qui fait insert au lieu d'update (l.488).

**Équipe** : Michele (CTO/lead), Diego (senior, code reviews + ADR validation), Kyle, Patrick, Lucas, Rémy. Antje pour validation wording front. **Diego absent cette semaine.**

**Format docs** : ticket Notion EN (Context / Acceptance Criteria / Scope & Boundaries / Implementation Notes / Dependencies), GitHub permalinks pinned à commit SHA, pas d'em-dashes.
```

---

### [M2] nouveau — `project_portfolio.md`

**Projet** : obsidian-vault
**Pattern** : 3 recaps consécutifs (23, 24, 25/04) sur un projet jamais documenté.

**Contenu proposé** :

```
---
name: Projet portfolio-game-boy-next
description: Portfolio perso de Rémy — interface rétro GameBoy en Next.js, déployé sur remy-shift.dev
type: project
---
Projet personnel actif : portfolio en Next.js 16 + next-intl FR/EN + framer-motion, interface rétro GameBoy (BootAnimation, écran CRT, parallaxe, sons rétro `retroAudio.ts`, curseur pixel art). Stack : Next.js 16 (proxy.ts), TypeScript strict, Vitest + Testing Library, ESLint, Vercel + Speed Insights, Resend pour le formulaire contact.

**Why:** Vitrine pro pour la mission Oli's Lab et la candidature talk LyonCraft. Aussi terrain d'exploration craft (TDD, a11y, sécurité headers, CSP).

**How to apply:** cwd typique = `~/Desktop/everything/Code/portfolio-game-boy-next/`. Quand on travaille dessus : TS strict utilisé (contraste avec olis-lab), tests systématiques, focus sur a11y / SEO / Lighthouse. Branche dédiée `gameboy-3d` (react-three-fiber, non entamée). Audit pages EN à finir.
```

---

### [M3] update — `user_role.md`

**Projet** : obsidian-vault (global)
**Pattern** : 3 recaps portfolio montrent que Rémy travaille **activement** sur Next.js 16 + TypeScript en parallèle d'olis-lab.

**Modification** :

```
AVANT :
Stack : React (migration vers Next.js), Node/Express, Payload CMS, React Query. **Pas de TypeScript** — dette technique forte. Tests en cours d'introduction : Vitest (unitaires) + Playwright (E2E) sur checkout-guest — premier périmètre testé.

APRÈS :
Stack mission Oli's Lab : React (migration vers Next.js), Node/Express, Payload CMS, React Query. **Pas de TypeScript** sur olis-lab — dette technique forte. Tests Vitest + Playwright en cours d'introduction (checkout-guest).

Stack projet perso (portfolio-game-boy-next) : Next.js 16, TypeScript strict, next-intl, framer-motion, Vitest + Testing Library, déployé Vercel sur `remy-shift.dev`. Discipline craft poussée (TDD, a11y, headers sécurité).
```

---

### [M4] nouveau — `feedback_doc_first_olis_lab.md`

**Projet** : obsidian-vault
**Pattern** : 2 sources distinctes — recap olis-lab 21/04 + conversation 18/04 ADR + confirmation indirecte par le Tech Weekly 25/04 (les PRs CMS sont triées par dépendances, format ticket Notion mentionné comme standard d'équipe).

**Contenu proposé** :

```
---
name: Documenter avant d'implémenter (Oli's Lab)
description: Sur olis-lab — produire audits + décisions + ADR avant tout plan d'implémentation
type: feedback
---
Pour les chantiers olis-lab non triviaux : produire d'abord les documents (audits état actuel + notes de décision + questions ouvertes), pas un plan d'implémentation.

**Why:** Tendance documentée à sauter au code trop vite. La mission Oli's Lab impose des arbitrages avec Michele/Diego avant l'implémentation. Format ticket Notion (Context / Acceptance Criteria / Scope & Boundaries / Implementation Notes / Dependencies), audits avec GitHub permalinks pinned à un commit SHA (pas de file:line qui pourrissent), langue **anglaise** (audience Michele + Diego), pas d'em-dashes, phrases factuelles.

**How to apply:** Quand Rémy demande un plan sur olis-lab — d'abord clarifier s'il manque des décisions d'archi en amont. Si oui : proposer un audit + une note de décision avant le plan d'implémentation. Format documents = ticket Notion EN. ADR rétrospectifs sur décisions déjà prises sont valables (formaliser l'implicite).
```

---

## Partie 2 — Enrichissement Notes Permanentes

### [V1] complétion — `Projets/Oli's Lab/Standards & Patterns.md`

**Source** : recap olis-lab 20/04 (pattern logging) + recap 21/04 (GitHub permalinks + format docs) + Tech Weekly 25/04 (priorisation PRs par dépendance).
**Justification** : Le fichier est déjà la référence pour les standards Oli's Lab. Trois standards nouveaux et durables ne sont pas documentés : (1) logging "log au sommet", (2) audits avec GitHub permalinks pinned à commit SHA, (3) format docs ticket Notion EN.

**Ajout proposé** (nouvelle section avant "Process & PRs") :

```markdown
## Logging serveur

Logger uniquement au sommet (controller), jamais aux niveaux intermédiaires (utils → adapter → service). Les erreurs remontent enrichies, le controller capture `error.stack`.

**Why:** Sur Lambda, chaque `console.error` déclenche une alerte Slack individuelle. Pattern "log + rethrow" à chaque couche = 1 erreur racine → 6 alertes. Constaté sur le flow `sync-all-live-products` (20/04).

```ts
// ❌ couche utils/adapter/service
console.error('Bigblue failed', error)
throw error

// ✅ controller uniquement
} catch (error) {
  console.error('syncAllLiveProducts failed', error.stack)
  return res.status(500).json({ error: error.message })
}
```

## Documentation technique

### Audits
GitHub permalinks pinned à un commit SHA (`https://github.com/.../blob/<sha>/path#L42`), jamais de références locales `file:line`. Les liens ne pourrissent pas dans le temps.

### Format des notes de décision
Template ticket Notion : **Context / Acceptance Criteria / Scope & Boundaries / Implementation Notes / Dependencies**. La note EST le ticket d'action, pas un méta-plan.

Audits, décisions, plans = **anglais** (audience Michele + Diego). Pas d'em-dashes (—), phrases factuelles, ton parlé.

### Documenter avant d'implémenter
Pour les chantiers structurants : audit état actuel + note de décision + questions ouvertes **avant** le plan d'implémentation. ADR rétrospectifs valables (formaliser l'implicite).

### Priorisation des PRs
Les PRs CMS sont triées dans Notion par ordre de dépendance (ex : brands → bridge brands → products → bridge products). Ajouter les priorités à chaque PR.
```

---

### [V2] nouvelle-note — `AudioContext mobile et autoplay.md`

**Dossier cible** : `Notes Permanentes/Code/Dev Knowledge/Webdev knowledge/`
**Sources** : recap portfolio-game-boy-next 23/04 + 24/04.
**Justification** : Trois découvertes techniques durables, non triviales, valables au-delà du portfolio. Aucune note web audio dans le vault à ce jour.

**Contenu proposé** :

```markdown
---
tags: [webaudio, mobile, ios, webkit]
---

# AudioContext mobile et autoplay

## Politique autoplay
Premier `AudioContext` au montage = muet sans geste utilisateur. Solution standard : déclencher / `resume()` au premier `pointerdown` ou `touchstart`. Pas de hack viable.

## Bug WebKit : `resume()` ne résout pas
Sur iOS Safari, `audioContext.resume()` retourne parfois une Promise qui ne se résout **jamais**. Symptôme : son de boot manque 1 fois sur 2 sur iPhone.

Solution robuste : double déclencheur avec flag de déduplication.

```ts
let played = false
const play = () => {
  if (played) return
  played = true
  playWelcomeChime()
}

audioContext.addEventListener('statechange', () => {
  if (audioContext.state === 'running') play()
})
audioContext.resume().then(play)
```

## Scheduling mobile : ~80ms minimum
Après `resume()` sur mobile, le moteur audio a besoin de ~80ms avant que `ctx.currentTime` avance de façon fiable. Schedule à 20ms de lookahead = notes scheduled dans le passé = silence.

```ts
const ATTACK_LOOKAHEAD_MS = 80 // 20ms ne suffit pas sur WebKit mobile
```

## DeviceOrientation : HTTPS strict obligatoire
`DeviceOrientationEvent.requestPermission()` ne déclenche **jamais** la popup iOS sur HTTP local (même via IP LAN). Pas d'erreur console, juste silencieux.

Pour tester : ngrok, Caddy reverse proxy, ou preview Vercel (HTTPS natif).
```

---

### [V3] nouvelle-note — `Tester un no-flash effect en React.md`

**Dossier cible** : `Notes Permanentes/Code/Programming Languages/js/React/Tests avec react/`
**Sources** : recap portfolio 25/04.
**Justification** : Pattern de test non évident, durable et réutilisable. La leçon "interroger le DOM final masque les bugs visuels de flash" est précieuse, applicable à tous les composants qui s'affichent puis disparaissent au mount client.

**Contenu proposé** :

```markdown
---
tags: [react, tests, vitest, ssr]
---

# Tester un no-flash effect en React

## Le piège
Un test qui interroge le DOM final (`expect(queryByText('boot')).toBeNull()`) **masque** les bugs de flash visuel. Si l'overlay s'affiche 1 frame avant d'être masqué par `useLayoutEffect`, l'utilisateur le voit, mais le test passe car au moment de l'assertion l'overlay est déjà retiré.

## La cause
`useLayoutEffect` est synchrone après le DOM update mais **après** le commit. Cycle render → commit → effect. L'utilisateur peut voir 1 frame entre commit et re-render.

## La fix code
`useState(initializer)` lit la valeur initiale **avant** le premier render :

```ts
// ❌ flash possible
const [showBoot, setShowBoot] = useState(true)
useLayoutEffect(() => {
  if (sessionStorage.getItem('boot-seen')) setShowBoot(false)
}, [])

// ✅ pas de flash
const [showBoot] = useState(() =>
  typeof window !== 'undefined' && !sessionStorage.getItem('boot-seen')
)
```

## La fix test
Spy sur le composant lui-même (mocké) pour capturer s'il a été monté **du tout**, pas juste son état final dans le DOM.

```ts
import { vi } from 'vitest'
vi.mock('./BootAnimation', () => ({
  default: vi.fn(() => null)
}))

import BootAnimation from './BootAnimation'

// Render Screen avec sessionStorage rempli
expect(BootAnimation).not.toHaveBeenCalled()
```

Assertion plus forte : "le composant n'a pas été monté", pas "le DOM final ne le contient pas".
```

---

## Partie 3 — Audit Vault

### Notes manquantes / suspectes (1)
- **`Vie Perso/Parcours & Narrative professionnelle.md`** : marquée comme appliquée dans le rapport 17/04 (V1) et le recap 18/04 mentionne sa "réécriture en sommaire de vie perso". Le fichier n'existe pas dans le vault au 25/04. Soit renommé/intégré ailleurs (probable : `Ma vie.md` ?), soit perdu. À vérifier — beaucoup de liens `[[Parcours & Narrative professionnelle]]` dans `LyonCraft 2026 - Talk.md` et `Workflow Claude + Obsidian.md` qui pointent dans le vide.

### Contenu potentiellement périmé (1)
- **`Vie Perso/Life Program 2024.md`** : déjà signalé dans le rapport 17/04, non traité. Toujours là, "2024" dans le titre en 2026.

### Activité non couverte (signaux)
- Le projet `portfolio-game-boy-next` a 3 recaps en 3 jours mais aucun memory file dédié. Couvert par `[M2]`.
- Korea/Learn/Grammaire/ : 17 notes coréennes actives, écosystème mature. Aucun memory `project_korea.md`. Pas une suggestion (la connaissance vit déjà dans le vault).
- Aucune note vault sur la migration CMS Payload côté chantier réel (seul `Standards & Patterns.md` couvre indirectement). Si tu veux une note chantier dédiée, c'est `Projets/Oli's Lab/Chantiers/` qui est le bon dossier — non rempli aujourd'hui.

---

## Pour appliquer

```
/evolve --apply 25-04-2026
```

Ordre recommandé : **M3 → M2 → M1 → M4 → V1 → V2 → V3**
(memory user_role d'abord pour poser le cadre stack, puis projet portfolio, puis chantiers olis-lab, puis standards, puis notes techniques)
