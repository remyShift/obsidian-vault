---
date: 20-06-2026
type: evolve-report
status: applied
applied_at: 20-06-2026 11:13
applied_ids: [M1, M2, V1, A6, A5]
skipped_ids: []
noted_ids: [A3]
resolved_ids: [A2, A1, A4]
tags: [evolve, meta]
correction: "A1 et A4 étaient des faux positifs (déjà actés dans project_ts_seed.md / project_ingredient_manager.md) — audit n'avait pas croisé les memory files. A5 corrigé dans Skills/wrap/SKILL.md."
---

# Evolve Report — 20-06-2026

**Dernier rapport** : 13-06-2026 (appliqué : M1, M2, M3 ; audit A1/A2/A3/A4 notés)
**Sources analysées** : 4 sessions olis-lab (15-06, 16-06, 17-06, 19-06), 5 sessions obsidian-vault (13-06 = méta-evolve écartée, 15-06, 19-06 x2, 20-06), 6 conversations (14-06 CLAUDE.md vault, 14-06 Korea, 17-06 LinkedIn/Tech'Work, 19-06 message équipe, 19-06 veille Tech'Work, 19-06 post bilan). 0 meeting nouveau (dernier = 01-06 Tech Weekly, < 13-06). Collecte AI Generated déléguée à 3 sous-agents (fan-out olis-lab / obsidian-vault / conversations).
**Notes Permanentes cartographiées** : 4 territoires (Code, Contenu & Créatif, Projets, Vie Perso), 894 fichiers. Lecture ciblée : memory olis-lab, build traps, note veille « Pimp my shell », structure Vie Perso + Veille TechWork + Korea, vérifications carry-over A1/A2/A4.
**Suggestions** : 3 (2 memory, 1 vault enrichissement) + audit (1 résolu, 3 carry-over, 2 nouveaux).

---

## Partie 1 — Memory Claude

### [M1] update — project_olis_lab.md
**Projet** : obsidian-vault
**Pattern** : 4 sessions olis-lab (15-06 → 19-06) convergent. Le bloc « Chantiers actifs (12-06) » est largement périmé : **presque tous les chantiers du 12-06 sont clos/mergés**, le **navbar global est passé de planifié à en implémentation**, et **2 chantiers nouveaux** apparaissent (footer global, génération/migration des slugs). En plus, un comportement Payload durable et réutilisable n'est pas documenté (relation orpheline → string).

Rappel `feedback_no_pr_tracking` : décrire les chantiers, **pas de numéros ni statuts de PR**. La granularité ci-dessous est volontairement au niveau chantier (« clos », « en review »), comme dans le rapport du 13-06.

**Contenu proposé** :

1. Ajouter une entrée à la liste « Conventions techniques durables (récurrentes) » :

```
- **Relation Payload vers un document supprimé = string brut, même à `depth >= 1`** : Payload ne peuple pas (et ne null pas) une relation orpheline, il renvoie l'ID en string. C'est ce qui force le narrowing de l'asserter et impose un choix EXPLICITE par couche : filtrer silencieusement (`flatMap` sur orphelins, pattern `products.ts`) vs throw explicite (`edits.ts`). Reco observée : throw sur `legacyId` manquant (donnée invalide), log sur orphelin (suppression légitime).
- **`legacyId` = ObjectId Mongo (24 hex)** posé par le script de sync ; sert d'`_id` au snapshot du cart. Un produit Payload neuf n'en a pas tant qu'il n'est pas synchronisé.
```

2. Remplacer le bloc « **Chantiers actifs (12-06)** » par :

```
**Chantiers (20-06)**:
- **Clos depuis le 12-06** (implémentés/mergés d'après les recaps 15-06 → 19-06) : SKU Design A (champ `required` + `validate`, sans bouton ni endpoint), announce bar (Payload global + CRA ; lecture Next quasi-mergée, dernier point en suspens), adminLabel picker cherchable, tests d'intégration `computeCartSnapshot`, fix délai redirection CRA→PDP Next.
- **Hook `computeCartSnapshot`** (raffiné depuis la fermeture des tests) : snapshot bilingue en+fr validé par `cartProductSchema` (le schéma exige `min(1)`, le hook tolère vide via `?? ''` → l'écart se règle dans les fixtures, pas dans le hook ni le schéma). Écriture du snapshot **uniquement sur update publié** (3 gardes : context `skipCartSnapshot`, `_status`, `operation`). `create` = no-op (TODO).
- **Navbar global** : passé de « plan validé, pas lancé » à **en implémentation/review**. Global Payload `navbar` créé, review Kyle traitée (typage `siblingData`, validateurs splittés, `staleTime`).
- **Footer global** (NOUVEAU) : plan complet rédigé en doc-first (schéma code + tableau Notion) avant implémentation. 2 décisions ouvertes : RBAC du champ `/target`, copyright figé en base vs préfixe en dur dans le code.
- **Génération / migration des slugs produits** (NOUVEAU, 17-06) : slugify NFD + strip des diacritiques + collapse des tirets (conserve le strip `/` et `.` existant). Migration minimale = détecter la locale source qui reproduit le slug, fallback préfixe marque + tiret. Inventaire 233 produits (206 OK, 27 redirects). Branche `fix/payload-slugs-generation`, tests intégrés (Rémy a demandé pas de tests auto).
- **RFC typage Payload→frontend** (`PAYLOAD_FRONTEND_TYPING_RFC.md`, racine repo, EN) : l'union `string | T` des relations n'est narrow ni par le SDK ni par la Local API quelle que soit la `depth`. Reco = wrapper SDK typé + Zod interne. Doc posée, suite = attente arbitrage équipe.
- **Bulk-add Products→Edit** : pas démarré (pivot append vs replace, code custom requis si append).
- **Page builder (blocks)** dans `apps/web` (Next.js), shop avant homepage : pas démarré.
- **Issus du Tech Weekly 25-05** : event tracking + consent mode (Kyle), SEO language repertoire, PDP/PLP SEO (Spark), perf Core Web Vitals, error page brandée Next.
```

---

### [M2] update — reference_olis_lab_build_traps.md
**Projet** : obsidian-vault
**Pattern** : 2 pièges build/UI apparus en session 15-06, dont un qui **précise** la ligne `importMap.js` déjà présente (régénération plus subtile que « après ajout d'un composant »).
**Contenu proposé** :

1. Remplacer la ligne `importMap.js` existante par une version précisée :

```
- **`importMap.js`** (`src/app/(payload)/admin/`) est gitignoré, régénéré au build (`pnpm generate:importmap`) — pas à committer. **La régénération ne change le fichier que si l'import est NOUVEAU** par rapport à l'existant ; un working tree sale donne un résultat trompeur (diff parasite ou no-op). Avant de régénérer, `git reset --hard origin/<branche>` pour partir propre, sinon le diff de l'importMap est ininterprétable.
```

2. Ajouter au bloc « How to apply » (sans supprimer l'existant) :

```
- **Tailwind v4 `@source` obligatoire côté Next** : pour que Tailwind génère les classes des composants UI importés (`@olis-lab/ui`), il faut déclarer leur chemin via `@source` dans le CSS. Sans `@source`, le composant est rendu mais **non stylé** (classes absentes du build). Piège silencieux : aucune erreur, juste un composant « nu ».
```

---

## Partie 2 — Enrichissement Notes Permanentes

### [V1] nouvelle-note — Setup Shell Mac
**Dossier cible** : `Notes Permanentes/Vie Perso/` (sibling de `Workflow Claude + Obsidian.md`)
**Source** : sessions obsidian-vault 19-06_22-01 + 20-06_10-58 (état FINAL du setup)
**Justification** : Rémy a explicitement demandé cette note (hot cache, prochaine étape « /evolve : la note Setup Shell Mac doit intégrer CETTE session »). **Elle n'existe PAS** : seule la note veille `Pimp my shell (Julien Wittouck)` existe (le talk Tech'Work, avec une checklist « À faire » désormais réalisée). Connaissance durable et réutilisable : le Brewfile reconstruit l'env, mais le **pourquoi** de chaque outil, le **câblage** fish et les **arbitrages** (outils écartés) ne vivent dans aucun fichier de conf. Lien direct avec le projet Corée (env portable à reconstruire avant le départ). Note manuelle (Notes Permanentes), pas AI Generated.

**Note** : pour ne pas créer une orpheline, relier à `[[Pimp my shell (Julien Wittouck)]]` (le talk source) et `[[Workflow Claude + Obsidian]]`. Ajouter un backlink depuis la note veille (voir audit A6).

**Contenu proposé** :
```
---
tags: [Perso, setup]
type: reference
date: 2026-06-20
---

# Setup Shell Mac

État final de mon environnement shell macOS, monté les 19-20 juin 2026 dans la foulée du talk [[Pimp my shell (Julien Wittouck)]] (Tech'Work). Tout est déclaré au Brewfile et versionné : `brew bundle` reconstruit l'environnement d'un coup. Critère portable = rebuild à l'identique sur une autre machine (utile pour le stage long à Séoul). Voir aussi [[Workflow Claude + Obsidian]].

## Principes

- Rien en `curl | sh`. Tout passe par Homebrew, déclaré dans le **Brewfile** (édité à la main, jamais `brew bundle dump --force`).
- Dotfiles sous **chezmoi** → repo privé `remyShift/dotfiles`. `chezmoi re-add` capture un changement local, push git pour synchroniser.

## Stack installée (rôle)

| Outil | Rôle |
|-------|------|
| Ghostty | émulateur de terminal (rendu GPU) |
| fish | shell (autosuggestions, complétions) |
| starship | prompt cross-shell contextuel |
| mise | runtimes + env + tâches. Remplace nvm/rbenv. Ne lit PAS `.nvmrc`, utilise `.mise.toml` |
| chezmoi | dotfiles versionnés / portables |
| splashboard | splash screen au démarrage |
| bat | `cat` boosté (coloration, pager) |
| lsd | `ls` boosté (icônes, couleurs) |
| zoxide | `cd` intelligent par fréquence |
| fd | recherche de **fichiers** (ignore `.gitignore`) |
| rg (ripgrep) | recherche de **contenu** (récursive) |
| fzf | fuzzy finder (câblé fish) |
| xh | client HTTP lisible |
| tldr (tealdeer) | aide par l'exemple |
| btop | moniteur système live (thème dracula bundlé) |
| gum | scripts shell interactifs (Charm) |

`fd` / `rg` et `xh` ne sont **pas** aliasés sur `find` / `grep` / `curl` : commandes en plus, pas remplacements.

## Câblage fish

- `config.fish` + `conf.d/aliases.fish` + `conf.d/gum.fish`.
- **fzf** câblé : `Ctrl+R` (historique) et `Ctrl+T` (fichiers) avec previews bat/lsd. `Alt+C` (saut de dossier) dormant sur Mac car Ghostty réserve Option aux accents → zoxide (`cd`) couvre le besoin.
- **Helpers git** : `gco` / `gbd` basculés en **fzf** (avec preview du log) ; `gadd` (multi-select) et `gcim` (input) restent en **gum**.

## Décisions / outils écartés

- **fzf recouvre gum** pour la sélection fuzzy → bascule `gco` / `gbd`. gum gardé pour le multi-select et l'input.
- Écartés à l'audit : direnv (couvert par mise), eza (lsd), dust/duf (ncdu), television (fzf), fastfetch/onefetch (splashboard), atuin (pas adopté).
- **fnox** retiré (un seul secret rare, pas justifié) — à reconsidérer si les secrets deviennent récurrents.

## Sur la table

- `yazi` (gestionnaire de fichiers TUI, à câbler avec zoxide/fzf).
- Pimper `starship.toml` (thème Dracula, icônes par langage).
```

---

## Partie 3 — Audit Vault

### Orphelines détectées (0 confirmées)
Audit ciblé (lecture non exhaustive). Préventif : la note V1 doit être reliée à `[[Pimp my shell (Julien Wittouck)]]` et `[[Workflow Claude + Obsidian]]` à la création, sinon elle naît orpheline.

### Liens cassés (0)
`Code/Veille/TechWork/Sommaire.md` relie correctement ses 4 notes de talk. `[[reference_olis_lab_build_traps]]` (dans project_olis_lab) existe. Pas d'autre vérification ciblée cette passe.

### Contenu à améliorer / intégrité (6)

- **[A2 — RÉSOLU]** Le recap `22-05-2026_15-58.md` (contenu obsidian-vault, mal rangé sous olis-lab) a été **déplacé vers `AI Generated/Sessions/obsidian-vault/`**. Plus rien à faire — carry-over fermé.

- **[A1 — RÉSOLU, faux positif]** `Notes Permanentes/Projets/ts-seed/` ne contient que `TS-Seed.md` parce que Rémy a **supprimé volontairement** `Design Document.md` et `Implementation Plan - Phase 1.md` (cohérent avec l'émergence par TDD). Déjà acté dans le memory `project_ts_seed.md` (« ne pas les recréer ni les signaler comme perdus »). L'audit de cette passe a eu tort de le re-flagger : il n'avait pas croisé le memory file. Rien à faire.

- **[A4 — RÉSOLU, faux positif]** Les 4 notes ingredient-manager ne sont pas dans `Notes de Lecture/Inbox/` parce que Rémy les a **rangées dans le dossier projet** : `Notes Permanentes/Projets/Oli's Lab/Scientific/Rating Tool/` (Overview, Architecture en place, Le système de scoring, Critique archi - Clean Code & DDD). Chemin déjà documenté dans le memory `project_ingredient_manager.md`. Même cause que A1 : l'audit n'a pas croisé le memory. Rien à faire.

- **[A3 — carry-over, léger]** `Notes de Lecture/Code/` : wikilinks vers Notes Permanentes jamais vérifiés. La politesse coréenne (31-05) semble déjà couverte (notes Particules + Formules de politesse présentes). Le reste à passer manuellement.

- **[A5 — APPLIQUÉ]** Le skill `Skills/wrap/SKILL.md` utilisait le **cadratin `—`** dans ses templates de nom de fichier (lignes 14, 42, 66) et son titre H1 (ligne 82), ce qui produisait des fichiers `DD-MM-YYYY — Titre.md` en violation de la règle CLAUDE.md « jamais de cadratin » (actée le 14-06). **Corrigé** : tous les `—` normalisés en `-` (y compris la prose interne du skill), et la règle ligne 114 reformulée sans montrer le caractère. Les anciens fichiers de `Conversations/` ne sont pas renommés (convention vault : on ne touche pas l'historique).

- **[A6 — NOUVEAU, enrichissement léger]** La note veille `Pimp my shell (Julien Wittouck)` a une section « À faire » désormais **entièrement réalisée** (setup monté) et des « noms à confirmer » résolus (Fnox = fnox, Splash Board = splashboard). À la création de V1, ajouter un backlink `[[Setup Shell Mac]]` dans cette note veille (« setup réalisé, voir … ») et cocher/archiver la checklist.

---

## Pour appliquer
```
/evolve --apply 20-06-2026
```
