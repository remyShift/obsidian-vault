---
date: 28-06-2026
type: evolve-report
status: applied
applied_at: 28-06-2026 11:42
applied_ids: [M1, M2, V1]
skipped_ids: []
noted_ids: [A1, A3]
resolved_ids: [A2]
tags: [evolve]
correction: "M2 tranché par Rémy = même projet restructuré (update project_ts_seed.md + hook MEMORY.md). A2 clos par M2 (memory ts-seed recalé sur la réalité seed4t-perso). A1 (dossier vide {project-name}) laissé volontairement, à gérer manuellement. V1 reliée via backlink depuis TDD.md (non orpheline)."
---

## Evolve Report — 28-06-2026

**Dernier rapport** : 20-06-2026 (appliqué : M1, M2, V1, A5, A6 ; A2/A1/A4 résolus en faux positifs ; A3 noté). Dans la fenêtre de 14 jours → seules les sources créées/modifiées depuis le 20-06 sont traitées.

**Sources analysées** : 5 sessions olis-lab (22-06, 23-06, 24-06, 25-06, 26-06), 2 sessions obsidian-vault (21-06, 24-06), 3 sessions seed4t-perso (22-06, 23-06, 24-06), 3 conversations (21-06 Julien Wittouck, 22-06 routine sport, 23-06 logements Séoul). 0 meeting nouveau (dernier daté ≥ 20-06 : aucun). Collecte AI Generated déléguée à 4 sous-agents Explore en parallèle (olis-lab / obsidian-vault / seed4t-perso / conversations).

**Notes Permanentes cartographiées** : 4 territoires, ~894 fichiers. Lecture ciblée : memory project_olis_lab / project_ts_seed / user_learning_trajectory, structure Code/Craft (TDD, DDD), Vie Perso (Korea, sport), vérifs backlinks Setup Shell + Routine Sport.

**Memory Claude vérifiés** : 3 projets (obsidian-vault, olis-lab, Winalia) + CLAUDE.md global.

**Suggestions** : 3 (2 memory, 1 vault enrichissement) + audit (3 items, dont 1 carry-over).

> Note Winalia : le projet a un dossier memory (`feedback_no_em_dash.md`) mais 0 session AI Generated. C'est un ancien audit tech (`Notes de Lecture/Audit Tech - Winalia.md`, 26-04), pas un chantier actif. Rien à faire côté memory.

---

### Partie 1 — Memory Claude

#### [M1] update — project_olis_lab.md

**Projet** : obsidian-vault

**Pattern** : 5 sessions olis-lab (22-06 → 26-06) convergent. Le bloc " Chantiers (20-06) " est déjà périmé sur plusieurs lignes (slugs passés de " branche en cours " à mergés via slug natif, checkout legacy supprimé, navbar mergé, footer routing tranché), **2 chantiers nouveaux** apparaissent (RBAC API, feature flags PostHog server/client), et **3 comportements durables et réutilisables** ne sont pas documentés (course `beforeChange`, PostHog cold-start, divergence index Mongo local/CI).

Rappel `feedback_no_pr_tracking` : granularité au niveau chantier (" mergé ", " revert + replan "), **aucun numéro ni statut de PR**.

**Contenu proposé** :

1. Ajouter à " Conventions techniques durables (récurrentes) " :

```
- **Hook de champ async vs validation `required` d'un champ frère** : Payload exécute les hooks de champ d'un `beforeChange` **en parallèle** (`Promise.all`). Un hook de champ async (ex : génération de slug) perd la course contre la validation `required` du champ frère → générer la valeur au **niveau collection `beforeValidate`** (séquentiel, async-safe), pas au niveau champ. Complète la convention « champ `required` ≠ remplissable par un hook serveur seul ».
- **`slugifyHandler` natif (`@payloadcms/next`) lit `field.custom.slugify`** de n'importe quel champ : réutilisable pour générer autre chose qu'un slug (toute valeur dérivée d'un champ source) sans nouvel endpoint. Slug natif Payload 3.84 = `slugField` (row checkbox + texte, composant `SlugField`, position défaut `sidebar`) ; le lock UI est un `useState` client éphémère (non persisté).
- **Feature flag PostHog : éval serveur ≠ éval client** : éval serveur avec id constant = bascule on/off déterministe (migration globale) ; ciblage par-utilisateur futur = refetch React Query côté client → découpler les deux usages. Piège : `onlyEvaluateLocally: true` renvoie `undefined` au cold start (poll async non awaité) ; retirer l'option fait retomber sur l'endpoint distant (self-heal). `NODE_ENV` est le vrai signal dev/prod (`next dev` = development, build Amplify = production) → pas d'env var ad-hoc.
- **Index Mongo `unique` : divergence local/CI réelle** : un index unique peut ne pas être appliqué sur une DB de test locale fraîche mais l'être en CI → un test vert en local peut casser en CI. À garder en tête sur les tests qui dépendent d'une contrainte d'unicité.
```

1. Remplacer le bloc " **Chantiers (20-06)** " par :

```
**Chantiers (28-06)**:
- **Slugs produits** : MERGÉ. Bascule sur le **slug natif Payload 3.84** (`slugField`) + `slugifyHandler` lisant `field.custom.slugify`, génération au niveau collection `beforeValidate` (course `beforeChange` évitée). Pas de migration des slugs accentués existants.
- **SKU** : raffiné. Bouton Generate masqué si `status === 'Live'` (clé BigBlue, risque off-stock) → SKU non régénérable sur Live ; pas de migration des SKU accentués.
- **Checkout legacy CRA** : SUPPRIMÉ entièrement (merge important). Next `/secure-checkout` 100% en prod, pas de rollback.
- **Navbar global** : MERGÉ. Le routing n'est pas stocké (`app` = legacy/next/external dérivé côté front en code).
- **Footer global** : `target` éditable côté CMS (mix lien interne/externe), décision routing tranchée vs la navbar.
- **Hook `computeCartSnapshot`** : la réintroduction a été revertée (QA manuelle impossible en dev, pas un bug du code) puis re-planifiée sans modif du hook. Comportement conservé : pas de snapshot si `legacyId` absent. `create` = no-op (TODO). Vrai fix du guard `beforeValidate` repoussé en follow-up.
- **RBAC** (NOUVEAU) : ~13/18 collections en `read: () => true`, aucun gating `create/update`, protections admin-UI only (`sku` readOnly, `notionId` hidden) **sans protection API**. RFC proposée = modèle minimal de rôles + helpers réutilisés. Distinct des feature flags PostHog (client, end-user-facing) qui ne sont PAS du RBAC.
- **Feature flags PostHog** (NOUVEAU) : architecture serveur-side (`lib/posthog-server`, singleton) + gate client (`useActiveFeatureFlags`). Utilisé pour des bascules de migration end-user-facing.
- **Reste ouvert** : bulk-add Products→Edit (pivot append/replace), page builder blocks `apps/web`, et les items du Tech Weekly 25-05 (event tracking + consent mode, SEO language repertoire, PDP/PLP SEO, Core Web Vitals, error page brandée Next).
```

---

#### [M2] update — project_ts_seed.md

**Projet** : obsidian-vault

**Pattern** : 3 sessions `seed4t-perso` (22-06 → 24-06) décrivent une incarnation du projet qui **diverge nettement** du memory actuel. Le memory dit " Next.js 16 App Router fullstack, npm, TS5, terminologie Brick/BrickBuilder/BrickContext/Patch/ProjectWriter, dossier `Code/ts-seed/` ". Le travail réel est un **monorepo pnpm** centré sur un package domaine, TS6, Vitest4, ESLint10 type-checked, Node 26, husky/commitlint, CI GitHub Actions. Terminologie réduite à Brick/Catalog/Cart + " grappe " + Recipe.

> ⚠️ Ambiguïté à trancher par Rémy à l'`--apply` : `seed4t-perso` est-il **le même projet que ts-seed** (qui a été restructuré/réécrit) ou une **copie perso distincte** ? Le digest parle de " copie perso " et il existe aussi d'anciens dossiers sessions `seed4j` / `seed4t`. Si c'est le même projet → update du memory ci-dessous. Si c'est un projet distinct → créer un nouveau memory `project-seed4t-perso.md` et laisser `project_ts_seed.md` tel quel.

**Contenu proposé** (en supposant : même projet, restructuré) :

```
Rémy builds **seed4t** (alias ts-seed), a personal TypeScript project bootstrapper inspired by Seed4J (Java). Repo public https://github.com/remyShift/ts-seed ; sessions sous `seed4t-perso`.

**Stack & archi**: monorepo **pnpm**, package domaine `packages/domain` (package name `@seed4t/core` — dossier non aligné sur le nom, mismatch en attente). TypeScript 6 Strict+, Vitest 4, ESLint 10 (flat config) + typescript-eslint 8.62 type-checked, Prettier, husky 9 + lint-staged + commitlint, Node 26. CI GitHub Actions (lint → format:check → build → test:run), protection main squash-only + CI bloquante. Architecture hexagonale émergente : 1er port (`IVersionResolver`) prévu seulement à T8 (Phase 2), dossier hexagonal en Phase 4 — pas de design upfront.

**Terminologie**: Brick (= package), grappe (= brick + ses deps), Catalog, Cart, Recipe (value object qui sérialise le `package.json` de sortie).

**Décisions domaine durables**:
- Dépendances stockées **par nom** (`string[]`) = clé étrangère vers le Catalog (source de vérité). Copier l'objet Brick = dupliquer la source de vérité = bug. 
- Résolution transitive = parcours récursif pre-order avec anti-cycle (`visited` Set) + dédup natif.
- `.tool-versions` est la source unique du Node (mise ne lit pas `.nvmrc` : `idiomatic_version_file_enable_tools = []`).

**État (28-06)**: Phase 1 DONE (résolution transitive, anti-cycle, dédup, validation fail-fast), 14 tests verts. Priorité suivante = Phase 3 (output `package.json` via Recipe) puis Phase 2 (versions « latest » + ranges).

**How to apply:** approche TDD naive-first (strings brutes d'abord, abstractions au refactor seulement). Claude = mentor/revue, jamais de code direct, pas de co-signature. Doc dans `Notes Permanentes/Projets/ts-seed/` (seul `TS-Seed.md` subsiste, suppressions volontaires — ne pas recréer).
```

---

### Partie 2 — Enrichissement Notes Permanentes

#### [V1] nouvelle-note — Les faux amis du test (présence, absence, unicité)

**Dossier cible** : `Notes Permanentes/Code/Craft/TDD/`

**Source** : session seed4t-perso 24-06-2026_10-57.md

**Justification** : connaissance de craft durable et réutilisable hors contexte, dans une catégorie déjà très investie (Red/Green/Refactor, Outside-In). Aucune note existante ne couvre les pièges d'assertion de test. Principe général, pas un bug fix one-shot : " un test jamais vu rouge ne prouve rien " est un invariant TDD. Atomique, reliable à `[[Red]]` et `[[Refactor]]`.

**Contenu proposé** :

```
---
tags: [Code, Craft, TDD]
type: permanent
---

# Les faux amis du test (présence, absence, unicité)

Une assertion qui passe ne prouve pas ce qu'on croit. Le vrai filet de sécurité, c'est un test qu'on a **vu rouge au moins une fois** : un test jamais vu échouer ne garantit rien (il peut valider une tautologie).

## `arrayContaining` ne teste que la présence

`expect(result).toEqual(expect.arrayContaining([a, b]))` vérifie que `a` et `b` sont **présents**. Il ne teste **ni l'absence** d'autres éléments, **ni les doublons**, **ni l'ordre**. Pour ces propriétés il faut une assertion explicite :
- absence → `expect(result).not.toContain(x)` ou comparer la longueur.
- unicité / pas de doublon → `expect(result).toHaveLength(n)` ou `expect(new Set(result).size).toBe(result.length)`.
- égalité stricte du contenu → `toEqual` sur le tableau complet.

## Réflexe

Avant de faire passer un test au vert, le casser exprès pour confirmer qu'il échoue pour la bonne raison (cf [[Red]]). Au [[Refactor]], se demander quelle propriété l'assertion ne couvre PAS encore.
```

> Candidat secondaire (non formalisé) : note " Récursion = 3 piliers " (geste répété + frein/`visited` + condition d'arrêt). Plus algo générique que craft : à voir si Rémy le veut dans `Code/Dev Knowledge/`.

---

### Partie 3 — Audit Vault

#### Orphelines détectées (0)

Les deux notes permanentes récentes sont reliées : `Code/Setup Shell.md` ← `Code/Code.md` + `Pimp my shell (Julien Wittouck).md` (backlink A6 du 20-06 bien appliqué) ; `Vie Perso/Routine Sport Full Body.md` ← `Vie Perso/Ma vie.md`. Rien à signaler.

#### Liens cassés (0)

`Setup Shell.md` → `[[Pimp my shell (Julien Wittouck)]]` (cible existe). `Routine Sport Full Body.md` = 0 lien sortant (note autonome, OK). Pas d'autre vérif ciblée cette passe.

#### Contenu à améliorer / intégrité (3)

- **[A1 — NOUVEAU, léger]** Dossier vide parasite `AI Generated/Sessions/{project-name}/` (créé le 13-04, jamais peuplé : placeholder de template figé). À supprimer manuellement (pas d'auto-fix). Cohérent avec " le vault n'est pas un dépotoir ".

- **[A2 — NOUVEAU, dépend de M2]** Le memory `project_ts_seed.md` décrit `Code/ts-seed/` + Next.js fullstack, mais le travail réel se fait dans `seed4t-perso` (monorepo pnpm, package domaine). Item à traiter via M2 (memory), pas via une note du vault : listé ici seulement pour traçabilité. Si Rémy tranche " projet distinct ", l'item devient un nouveau memory, pas une correction.

- **[A3 — carry-over léger]** `Notes de Lecture/Code/` : wikilinks vers Notes Permanentes jamais vérifiés exhaustivement (déjà noté le 20-06). À passer manuellement quand l'occasion se présente. Pas de preuve fraîche de casse cette passe.

#### Audit clos (ne plus re-signaler)

Registre cumulatif relu par la Phase 1a. Recopié du rapport 20-06 + rien de nouvellement clos cette passe.

- `Sessions/olis-lab/22-05-2026_15-58.md` mal rangé - clos le 20-06-2026 : déplacé vers `Sessions/obsidian-vault/`.
- `Projets/ts-seed/` Design Document + Implementation Plan absents - clos le 20-06-2026 : supprimés volontairement par Rémy (acté dans `project_ts_seed.md`), ne pas recréer.
- Notes de lecture ingredient-manager absentes de `Inbox/` - clos le 20-06-2026 : rangées dans `Projets/Oli's Lab/Scientific/Rating Tool/` (acté dans `project_ingredient_manager.md`).
- Skill `wrap` cadratin dans les templates - clos le 20-06-2026 : normalisé en tiret simple (A5 appliqué).
- Backlink `[[Setup Shell]]` depuis la note veille " Pimp my shell " - clos le 20-06-2026 : ajouté (A6 appliqué).
- Memory `project_ts_seed.md` décrivait Next.js fullstack alors que le réel est `seed4t-perso` (monorepo pnpm, `@seed4t/core`) - clos le 28-06-2026 : memory recalé via M2 (Rémy a tranché " même projet restructuré ").

---

### Pour appliquer

```
/evolve --apply 28-06-2026
```
