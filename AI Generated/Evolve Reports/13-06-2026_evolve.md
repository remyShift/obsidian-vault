---
date: 13-06-2026
type: evolve-report
status: applied
applied_at: 13-06-2026 11:55
applied_ids: [M1, M2, M3]
skipped_ids: []
noted_ids: [A1, A2, A3, A4]
tags: [evolve]
---

# Evolve Report — 13-06-2026

**Dernier rapport** : 05-06-2026 (appliqué)
**Sources analysées** : 5 sessions olis-lab nouvelles (05-06_15-55, 08-06, 09-06, 10-06, 12-06), 1 session nouveau projet ingredient-manager (08-06), 1 session obsidian-vault (08-06, passe evolve méta). 0 conversation nouvelle (dernière = 31-05). 0 meeting nouveau (dernier = 01-06 < 05-06, exclu par le filtre de date).
**Notes Permanentes cartographiées** : structure DDD/Craft relevée ciblée ; vérifications carry-over A1/A2 + recherche notes ingredient-manager.
**Suggestions** : 7 total (3 memory, 0 vault enrichissement, 4 audit)

---

## Partie 1 — Memory Claude

### [M1] update — project_olis_lab.md
**Projet** : obsidian-vault
**Pattern** : 5 sessions olis-lab (05-06 → 12-06). La section « Chantiers actifs (05-06) » est partiellement périmée et **une convention SKU est factuellement fausse** :
- **SKU** : bascule actée **Design B → Design A** (sessions 05-06 + 08-06). Le champ `sku` est repassé `required: true` (type `string`) + `validate` custom qui relâche à la création, **PAS read-only/auto**. Le **bouton "Generate SKU" et l'endpoint `/api/generate-sku` ont été SUPPRIMÉS** (génération uniquement au save serveur). La memory actuelle dit « champ read-only/auto, fonction pure buildSku (hook + endpoint) » → faux sur les deux points (plus read-only, plus d'endpoint).
- **Announce bar** : passée de « intégration CRA sous flag » à **quasi-terminée** (`isValidLink` extrait dans `apps/cms/src/lib/validateLink.ts`, colocation `TopBannerContainer`, inversion du fetch, regex lien externe centralisée dans `shared/routing.ts`, review Kyle approuvée 12-06). Reste le re-approve d'un reviewer + CI.
- **Nouveaux chantiers émergés non documentés** : (a) **adminLabel** dénormalisé pour rendre le relationship picker Payload cherchable (subcategories + products, 2 branches) ; (b) **tests `computeCartSnapshot`** 100% intégration + branchement schéma Zod `cartProductSchema` partagé ; (c) **fix délai redirection CRA→PDP Next** (overlay `isNavigatingAtom`, mergé) ; (d) **bulk-add Products→Edit** (planifié, append vs replace non tranché, code custom requis) ; (e) **RFC typage Payload→frontend** (`PAYLOAD_FRONTEND_TYPING_RFC.md`, stratégique, attente call équipe).
- **Navbar global** : inchangé (plan validé, `isValidLink` poussé, implémentation pas lancée).

Note `feedback_no_pr_tracking` : décrire les chantiers, pas de numéros/statuts de PR.

**Contenu proposé** :

1. Dans la liste « Conventions techniques durables », **ajouter 2 entrées** (principes durables, vérifiés sur plusieurs sessions) :

```
- **Asserter (lecture, throw) ≠ Resolver (écriture, fetch)** : deux primitives, une par couche/intention. Un asserter `assert.populated` **throw** si la relation n'est pas peuplée (garde de lecture) ; un resolver type `resolveBrand` **fetch** si pas peuplé (logique d'écriture, hook). Même check `typeof === 'object'`, intentions opposées → ne pas collapse l'un dans l'autre. Le try/catch + fetch (exception en flux nominal) est écarté.
- **Champ `required` Payload ≠ remplissable par un hook serveur seul** : la validation `required` tourne **côté client avant l'envoi de la requête**, donc aucun hook (beforeValidate/beforeChange) ne tourne à temps. Solutions : `required` + `validate` custom qui relâche à la création (le type généré reste `string`), OU génération client, OU non-required. Avec `versions.drafts`, les champs required sont bypass en draft.
```

2. Remplacer le bloc « Chantiers actifs (05-06) » par :

```
**Chantiers actifs (12-06)**:
- **SKU produit** : auto-génération du SKU (formule Notion) en hook `beforeValidate`, fonction pure `buildSku` (`apps/cms/src/collections/hooks/sku/`) source de vérité unique. **Design A retenu** : champ `sku` `required: true` (type `string`) + `validate` custom qui relâche à la création, **pas read-only**, **sans bouton ni endpoint** (génération serveur au save). Exception Collectibles détectée par ID de marque.
- **Announce bar** : quasi-terminée. Global Payload `announcement-bar` + CRA sous flag `dev_announcement_bar`, mécanique colocée dans `TopBannerContainer` (le hook `useTopBannerMessages` ne fetch plus, reçoit la query en param). `isValidLink` extrait dans `apps/cms/src/lib/validateLink.ts` (1er util de validation transverse, branché announce-bar + trading-plan + futur navbar). Regex lien externe centralisée dans `packages/shared/src/routing.ts`. Reste : merge + câblage TopBanner Next.js.
- **adminLabel (picker cherchable)** : un relationship picker Payload ne cherche que sur `useAsTitle` → champ `adminLabel` dénormalisé (localisé, `admin.hidden`, hook `beforeChange`) format `subcat > category`. 2 branches (subcategories + products), infra migrations créée (backfill par locale). Plugin Nested Docs écarté (hors sujet).
- **Tests hook cart** : `computeCartSnapshot` couvert 100% en intégration (vrai `payload.create/update`, Mongo local), schéma Zod `cartProductSchema` partagé branché côté producteur (`parse` hors try → throw bloque la save). Factory DB `tests/factories/product.ts`.
- **Fix délai CRA→PDP Next** : overlay feedback immédiat au clic (atom `isNavigatingAtom`) + `loading.tsx` côté Next (streaming du shell). Mergé.
- **Bulk-add Products→Edit** : feature planifiée. Le pivot = append vs replace (le bulk-edit natif Payload remplace le tableau `hasMany`, ne sait pas append) → code custom obligatoire si append. Pas démarré.
- **RFC typage Payload→frontend** (`PAYLOAD_FRONTEND_TYPING_RFC.md`, racine repo, EN) : l'union `string | T` des relations n'est narrow ni par le SDK ni par la Local API quelle que soit la `depth` (limitation universelle Payload). Reco = wrapper SDK typé (depth fixée par méthode) + validation Zod interne. Stratégique, attente call Kyle/Diego.
- **Navbar global** : plan validé, `isValidLink` poussé, implémentation pas lancée (top-level non éditable cette itération — arbitrage Michele).
- **Page builder (blocks)** : approche blocks dans `apps/web`, pas démarré.
- **Issus du Tech Weekly 25-05** : event tracking + consent mode (Kyle), SEO language repertoire, PDP/PLP SEO (Spark), perf Core Web Vitals, error page brandée Next.
```

---

### [M2] update — reference_olis_lab_build_traps.md
**Projet** : obsidian-vault
**Pattern** : 2 pièges build/DB récurrents apparus en sessions 09-06 (et croisés ailleurs), coûteux et non documentés.
**Contenu proposé** :

Ajouter au bloc « How to apply » (sans supprimer l'existant) :

```
- **Mongoose pluralise le slug de collection** : le slug `product-media` est stocké dans la collection Mongo `product-medias` (idem `logo-media`→`logo-medias`). Un insert/delete/findByID **DB brut** doit viser le nom **pluralisé** (sinon NotFound). Utile pour insérer une fixture média sans déclencher le pipeline upload Sharp (qui throw sur image synthétique).
- **`pnpm --filter @olis-lab/shared build` peut buter sur le check `engines.node`** : contourner en lançant `npx tsc` directement dans `packages/shared`. À recroiser avec le piège Node 20 déjà listé.
```

---

### [M3] create — project_ingredient_manager.md
**Projet** : obsidian-vault
**Pattern** : nouveau sous-projet réel de la mission Oli's Lab, inspecté en profondeur (session 08-06), absent de tous les memory files. Contexte émergent (critère Axe A.3). Stack et conventions **radicalement différentes** du reste de la mission (TS strict vs CRA sans TS) → mérite son propre fichier pour les futures sessions dans ce repo.
**Contenu proposé** :
```
---
name: Projet ingredient-manager
description: Sous-projet Oli's Lab — back-office de notation produits (scoring INCI/COSING), TS strict + Zod
type: project
---
**ingredient-manager** = le back-office de **notation des produits** Oli's Lab. Pipeline données → matching INCI/COSING → scoring via 10 règles (R01→R10, 6 bonus + 4 malus), barèmes skincare/bodycare distincts. Notion de **source** : `shop` (produits Oli's Lab) vs `ext` (`ewg`, `lancome`) pour benchmark sur la même grille.

**Stack** : TypeScript strict + Zod partout, Express 5, MongoDB driver natif, Next.js 14 servi par le même process Express. **Le projet le plus propre de la mission côté typage** (contraste avec le legacy React/Node sans TS). Bonnes pratiques en place : évaluateurs = fonctions pures, règles & datasets data-driven et versionnés (R05/R10 en 1.0.0/1.1.0), score auditable (`observed_inputs`).

**Why:** Codebase distincte du reste de la mission — ne pas y appliquer par réflexe les conventions du monorepo legacy (CRA/Payload). Ici c'est TS strict + Zod.

**How to apply:** Dette technique identifiée (documentée en notes de lecture — voir audit A4, notes actuellement absentes du vault) : `ext-scoring.repo.ts` = God file (3314 lignes), `scoring.repo.ts` (1625 lignes) ; abstraction par source qui **fuit** (`if (source === SHOP) … else …` remonte jusqu'à `scoring.service.ts`) ; logique de scoring dupliquée shop/ext car descendue dans la couche data ; dépendance inversée (évaluateur R01 importe `normalizeNameRule1` d'un repo Mongo = domaine→infra) ; modèle non unifié (`matches.cosingId` camelCase vs `extMatches.cosing_id` snake_case → c'est ce qui force le if/else). Plan refacto priorisé : (1) sortir le scoring des repos vers domaine/service, (2) `assertAllMatched` → méthode du port, (3) unifier le modèle `Match`, (4) découper `ext-scoring.repo.ts`, (5) remonter `normalizeNameRule1`, (6) cleanup logs Pino.
```

Ajouter aussi la ligne d'index dans MEMORY.md :
```
- [Projet ingredient-manager](project_ingredient_manager.md) — sous-projet Oli's Lab, back-office scoring produits, TS strict + Zod (codebase la plus propre de la mission), God files à refacto
```

---

## Partie 2 — Enrichissement Notes Permanentes

Rien de significatif détecté cette passe. Les nuggets techniques de la période (asserter vs resolver, required Payload, union de relation non narrow) sont **trop couplés à Payload/au contexte projet** pour une note permanente générique → routés en memory (M1) plutôt qu'en Notes Permanentes. Le principe craft « hexagone émerge du TDD » a déjà été ajouté au rapport précédent (V1 du 05-06).

Le seul vrai gisement de craft réutilisable de la période (critique Clean Code/DDD d'ingredient-manager : abstraction qui fuit, God file, violation dépendance domaine→infra) **vit dans des notes de lecture qui n'existent pas dans le vault** (voir audit A4) → impossible à promouvoir tant que ce n'est pas récupéré.

---

## Partie 3 — Audit Vault

### Orphelines détectées (0 confirmées)
Audit non exhaustif (lecture ciblée).

### Liens cassés (0)
`[[reference_olis_lab_build_traps]]` (référencé dans project_olis_lab.md) existe. Pas d'autre vérification ciblée cette passe.

### Contenu à améliorer / intégrité (4)

- **[A4 — NOUVEAU, intégrité]** Le recap `Sessions/ingredient-manager/08-06-2026_17-49.md` affirme avoir créé 4 notes de lecture dans `Notes de Lecture/Inbox/Ingredient Manager (Oli's Lab)/` (00 Vue d'ensemble, 01 Architecture, 02 Scoring, 03 Critique Clean Code/DDD) + une section au Sommaire des Notes de Lecture. **Aucune de ces notes n'existe dans le vault** (Inbox ne contient que `Rust/` + `_template`). Les 2 chemins vault sont un symlink → pas un faux négatif. Travail potentiellement perdu ou écrit hors vault. **À investiguer** : retrouver/recréer ces notes (la note 03 contient le seul matériau craft promouvable de la période).

- **[A1 — carry-over, toujours ouvert]** `Notes Permanentes/Projets/ts-seed/` ne contient que `TS-Seed.md`. Les recaps seed4j 25-05 référencent `Design Document.md` et `Implementation Plan - Phase 1.md` comme sauvegardés ici. Vérifier qu'ils ne sont pas perdus (sinon consolidés dans TS-Seed.md).

- **[A2 — carry-over, toujours ouvert]** `AI Generated/Sessions/olis-lab/22-05-2026_15-58.md` : contenu obsidian-vault (LinkedIn scoring Dataview) mais `project: olis-lab`, rangé dans le mauvais dossier. Déplacer vers `obsidian-vault/`.

- **[A3 — carry-over, toujours ouvert]** `Notes de Lecture/Code/` (créé le 16-05) : wikilinks vers Notes Permanentes jamais vérifiés. Notes mûres à promouvoir signalées en conversations 26-05 (Code Smells → `Code/Craft/`) et 31-05 (politesse coréenne → `Vie Perso/Korea/Learn/Grammaire/`). À passer manuellement.

---

## Pour appliquer
```
/evolve --apply 13-06-2026
```
